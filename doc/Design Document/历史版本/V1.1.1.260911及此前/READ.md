# 六路质量流量计控制系统工程记录

更新时间：2026-09-11。当前阶段：硬件接口核对与初步软件设计，本文不代表固件已经实现或完成实板验证。

**当前工程版本：V1.1.1.260911。** 从本版本开始，工程统一使用下述版本命名规则，并在本文件维护每个版本的变更记录。

## 版本命名与变更记录

版本格式为 `V主版本.次版本.修订版本.YYMMDD`，例如 `V1.1.1.260911`。前三段为正常递增的版本号，最后六位为该版本形成日期，`260911` 表示 2026 年 9 月 11 日。

- 主版本：整体架构或不兼容接口发生重大调整时递增。
- 次版本：新增功能或较大的方案扩展时递增。
- 修订版本：问题修复、细节调整或文档补充时递增。
- 默认采用以上递增规则；主版本或次版本递增时，其后的数字归零；用户指定的版本号优先。本次起始版本固定为 `V1.1.1.260911`。
- 每次形成新版本，必须同步更新本文件的当前版本号，并在下表新增日期、变更内容、关联文件及验证状态；保留历史条目，不覆盖旧版本记录。
- 同一天可以形成多个版本，通过前三段区分；日期使用各版本的实际形成日期，不因打开文件或跨日查看而改变。后续正式交付文件的版本标识采用同一格式。

| 版本 | 日期 | 变更内容 | 关联文件及验证状态 |
| --- | --- | --- | --- |
| V1.1.1.260911 | 2026-09-11 | 建立统一版本命名及记录制度，将当前六路流量计软件方案作为首个记录版本：上位机固定 CAN；MFC 下行 RS485 / CAN 编译选择并匹配跳线；RS485 方案采用 Modbus RTU；FreeRTOS 三个业务任务；V7/V9 同步且与 V8 互斥；补充 CAN 硬件通信和 EX201 专用 ASCII 手册说明。 | 本文件及 [软件设计补充稿](<doc/Design Document/流量计控制系统软件设计-草稿-V0.4.docx>)。该 Word 文件沿用本次统一命名前的 V0.4 文件名和内部修订号，作为本版本的方案依据；后续修订采用统一版本号。Word 已完成 25 页排版检查；尚未完成固件实现和实板联调，Modbus 寄存器表及 CAN 设备协议仍待确认。 |

此前的 V0.3、V0.4 为 Word 草稿历史修订号，不追溯改写为正式工程版本；当前工程版本以上述记录为准。

本项目使用 R7FA4M1AB3CFM 管理六台 EX-201S 系列质量流量控制器和九个外部电磁阀。流量闭环由流量控制器内部完成，MCU 负责流量设定、状态采集、气路过程协调及阀门联锁，上位机负责操作和监测。

本文将信息分为“原理图确认”“用户装配说明”“软件建议”和“待确认”。原理图能够证明设计连接，不能单独证明实际焊接、线束接法及器件工作状态。历史设计中将 RJ45 解释为以太网的方案已被本次核对结论取代；Word 软件设计草稿已于 2026-09-11 另存 V0.4 补充稿（流量计控制系统软件设计-草稿-V0.4.docx），其余既有 Word、Visio 文档仍需按本记录同步。

**2026-09-11 协议更新：用户确认流量计 RS485 使用 Modbus RTU。本项目采用 MCU 主站、六台流量计从站的方案，取代此前 EX201 专用 ASCII 协议假设。** RTU 类型已确认；波特率、校验位、停止位、设备地址、功能码及寄存器表仍待对应设备资料确认。

**2026-09-11 阀门联锁更新：V7 与 V9 同步开关，组成一个逻辑阀组，与 V8 互斥。** 当前联锁以这条用户最新说明为准，具体规则见第 8 节。

**2026-09-11 通信章节补充：** Word V0.4 新增 MFC CAN 硬件链路、MCP2515 初始化、SPI 轮询收发及异常处理；补充 EX201 现有手册的专用 ASCII 帧、校验和、常用指令与流量换算，作为资料核对依据。当前 RS485 方案仍为用户确认的 Modbus RTU；专用 ASCII 不能直接替代 RTU。实际设备的 Modbus 寄存器表、CAN 支持型号及报文协议仍待厂家确认。

## 1. 依据与当前结论

| 依据 | 用途 |
| --- | --- |
| [CVD01(Q2)-GS260806CN-V2.0B2.0.pdf](<doc/CVD01(Q2)-GS260806CN-V2.0B2.0.pdf>) | 第 1 页：上位机接口；第 2 页：MCU、SPI 转 CAN；第 3 页：流量计接口、跳线及阀门驱动 |
| [EX201 操作说明书.pdf](<doc/科赋乐质量流量计说明书/EX201 操作说明书.pdf>) | 流量计接口、使用条件及预热要求 |
| [EX201 说明书485通讯.pdf](<doc/科赋乐质量流量计说明书/EX201 说明书485通讯.pdf>) | 历史参考：内容为专用 ASCII 指令，不是本项目已确认的 Modbus RTU 寄存器规范；不能直接沿用其帧格式或串口参数 |
| [气路图](<doc/无标题.png>)及用户说明 | 六条气体支路、九阀用途与互斥要求 |
| 用户于 2026-09-10 提供的装配说明 | 上位机收发器位置只焊 U5；流量计外接端口按线束三选一使用 |
| 用户于 2026-09-11 确认的协议 | RS485 使用 Modbus RTU |
| 用户于 2026-09-11 更新的阀门逻辑 | V7/V9 同开同关，二者组成的阀组与 V8 互斥 |
| [Modbus 串行规范 V1.02](https://www.modbus.org/file/secure/modbusoverserial.pdf)、[应用协议 V1.1b3](https://www.modbus.org/file/secure/modbusprotocolspecification.pdf) | 通用 RTU 帧、串行时序、功能码和异常响应规则；不能替代厂家寄存器表 |

当前通信架构为：

| 通信方向 | 物理接口 | 总线与控制器 | 软件结论 |
| --- | --- | --- | --- |
| 上位机 ↔ MCU | J1、J2，两个 DB25 | 两口共用一路 CAN，MCU 内置 CAN0 + U5 | 当前装配固定使用 CAN，仅设一个上位机通信任务 |
| MCU ↔ 六台流量计 | J9、J15，两个 RJ45；J10，一个 DB9 | 三口共用一组 H/B、L/A；J17、J18 共同选择 RS485 或 CAN | RS485 采用 Modbus RTU；CAN 设备协议待定；编译后端与跳线保持一致 |

**本版原理图中的 RJ45 是串行总线连接器，没有显示以太网 MAC/PHY 或以太网接口电路，不按以太网、TCP/IP 或网口通信设计。**

两个上位机 DB25 属于同一条上位机总线；三个流量计端口属于同一条流量计总线。上下两条总线相互独立，即使两侧均使用 CAN，也不是直接连在一起的同一条 CAN 总线。

## 2. 上位机接口核对

### 2.1 连接关系

原理图第 1、2 页显示：

```text
上位机
  ↕
J1 / J2 DB25，共用 OUT/H/485B、OUT/L/485A
  ↕ 保护与滤波电路
U5：TCAN1042VDRQ1 CAN 收发器
  ↕ U4：ADUM1201 数字隔离
MCU：CAN_TXD0 / CAN_RXD0，内置 CAN0
```

原理图还画有 U2（SP3485EN）作为 RS485 路径。根据用户提供的实际装配情况，U2 未焊、U5 已焊，因此当前上位机接口按 CAN 使用。J1/J2 共用 U5，不能视为两个独立 CAN 通道；两个接口还带有电源等引脚，并非全部引脚都用于通信。

### 2.2 J1/J2 通信相关引脚

以下为原理图引脚编号；制作线束时须区分连接器插接面与焊接面的观察方向。

| 两个 DB25 的引脚 | 原理图网络 | 当前 CAN 用途 |
| --- | --- | --- |
| 2、15 | OUT/H/485B | CANH |
| 3、16 | OUT/L/485A | CANL |
| 1、14 | GND3 | 通信侧参考地 |
| 5、6、7、18、19、20 | 24VP | 电源网络，不能当通信脚使用 |

### 2.3 使能、终端电阻与装配注意点

- `RS485_EN0` 经 U3 控制 U5 的 STB。名称虽带 RS485，当前 CAN 版本仍需初始化此信号。
- `RS485_RES0` 经 U3、U6 控制 R16（120 Ω）终端电阻，根据本板在总线中的位置配置。
- U4 的 MCU 侧存在 CAN 引脚与备用 UART 引脚的电阻连接路径。须核对 R8、R9、R11、R13 的实际装配；CAN 使用期间，不能同时将备用 `RS485_TXD0` 配置为活动 UART 发送输出。
- U5 的 STB 低电平为正常通信，高电平为待机。MCU 到隔离侧的实际电平和上电默认状态需实测确认。芯片模式依据 [TI TCAN1042-Q1 数据手册，第 9.4 节](https://www.ti.com/lit/gpn/tcan1042-q1)。

## 3. 流量计接口核对

### 3.1 三个端口共用同一条总线

J9、J15、J10 的通信线均接到同一组 `H/B`、`L/A` 网络。三口之间没有接口选择开关，也没有各自独立的收发器。

用户要求的“RJ45 或 DB9，三选一”属于产品接线约定：使用其中一个端口接入六台流量计所在的总线。软件无需按 J9/J15/J10 分成三个驱动。其余端口在电气上仍连接同一条总线，不能另接一种协议的设备网络。

| 板端接口 | H/B | L/A | 其他相关引脚 |
| --- | --- | --- | --- |
| J9 RJ45 | 4 脚 | 5 脚 | 1、2 脚为 GND4；3、6、7、8 脚未接通信信号 |
| J15 RJ45 | 4 脚 | 5 脚 | 1、2 脚为 GND4；3、6、7、8 脚未接通信信号 |
| J10 DB9 | **1 脚** | **2 脚** | 3 脚为经保护的 24V5；4 脚为 GND5；5 脚为 FG；6、7、8、9 脚未接 |

**修正：J10 的 L/A 是 2 脚，7 脚未接。** 不能沿用此前将 7 脚写为通信脚的记录。

流量计说明书中的设备 RJ45 为 1/2 脚 TR_COM、4 脚 TR−、5 脚 TR+。板端 H/B、L/A 的命名不能直接代替器件极性核对，须结合下面的 0 Ω 电阻装配确认线束。

板上 J10 的定义也不能套用流量计本体 DB9 的定义：设备 DB9 承担电源和模拟控制等信号，不能假设一根 DB9 直通线即可完成数字通信。选用板端 DB9 时，应单独出具至设备通信接口的线束表。GND4、GND5、FG 的连接按隔离和接地方案核对，不默认短接。

### 3.2 J17/J18 的选择关系

| 工作方式 | J17 短接 | J18 短接 | 连接的收发器 |
| --- | --- | --- | --- |
| CAN | 1–2 | 1–2 | U22 的 CANH / CANL |
| RS485 | 2–3 | 2–3 | U17 经电阻配置后的 H_485B / L_485A |

J17 的 2 脚、J18 的 2 脚分别进入公共滤波与保护电路，再连到三个端口。两只跳线需要人为设置一致，并非机械联动开关。跳线方向以 PCB 的 1 脚标识为准，不能仅凭“上面/下面”判断。

图中没有用于读取 J17/J18 状态的 MCU 检测信号，因此固件不能自动知道跳线位置，也不能通过软件切换跳线。生产和调试时应核对“固件配置、跳线、器件装配、设备协议、线束”五项是否匹配。

U17 的 A/B 另有 0 Ω 电阻换向配置：R17 + R148 对应 B→H_485B、A→L_485A；R14 + R149 对应交叉连接。须核对实际焊接，四只不能全部导通，否则会将 A/B 相连。

### 3.3 RS485 路径

当前协议为 Modbus RTU。MfcTask 作为主站事务调度者，经 SCI2 控制 RS485 收发；六台流量计作为从站响应请求。

```text
MCU SCI2：MFC_TXD2 / MFC_RXD2
  ↕ U21 ADUM1201
U17 SP3485EN
  ↕ A/B 电阻配置
J17、J18 均短接 2–3
  ↕ 公共滤波、保护电路
J9 / J15 / J10 → 六台流量计的共享总线
```

`MFC_EN2` 用于收发控制相关电路；`MFC_RES2` 控制共用终端电阻。半双工发送必须等待 UART 最后一个停止位真正发送结束，再按经实板验证的控制电平切换接收，不能仅以发送缓冲区为空作为切换条件。

### 3.4 CAN 路径

```text
MCU SPI：MOSI / MISO / SCK / NSS
  ↕
U11 MCP2515-I/ST：独立 CAN 控制器
  ↕ MFC_CTXD / MFC_CRXD，经 U23 ADUM1201
U22 TCAN1042VDRQ1：CAN 收发器
  ↕
J17、J18 均短接 1–2
  ↕ 公共滤波、保护电路
J9 / J15 / J10 → 支持相应 CAN 协议的流量计总线
```

- U11 完成 SPI 与 CAN 控制器之间的接口功能，U22 完成 CAN 物理层收发，两者功能不同。
- U11 的 Y1 标为 **8 MHz**。CAN 位时序计算应使用该振荡器频率，不能套用常见 16 MHz 模块的参数。
- 第 2 页中 U11 的 **INT（13 脚）未画出到 MCU 的连接**，RX0BF/RX1BF 同样未接。因此当前板型先采用 SPI 定时读取接收/错误状态的方案，不能假定已有 MCP2515 外部中断通知。
- U11 RESET（19 脚）由 R26/C44 构成上电复位相关电路，未显示 MCU 复位控制 GPIO。
- MCP2515 是经典 CAN 控制器。它只有两个接收缓冲区，SPI 轮询周期须结合 CAN 波特率、过滤规则、最大突发报文量和任务调度延迟验证。不能因为收发器支持更高能力就将本系统定义为 CAN FD。依据 [Microchip MCP2515 数据手册](https://ww1.microchip.com/downloads/en/DeviceDoc/MCP2515-Stand-Alone-CAN-Controller-with-SPI-20001801J.pdf)。

### 3.5 MFC_EN2 与 MFC_RES2：两种版本都需要保留

原理图第 3 页中，`MFC_EN2` 经 U7 同一只光耦晶体管影响两个**不同的电气节点**：

| U7 节点 | 网络与作用 |
| --- | --- |
| 12 脚，集电极 | MFC/EN，经 R98 到 U17 的 DE 和 /RE；R100 上拉至 5V4 |
| 11 脚，发射极 | CAN/STB，到 U22 的 STB；R113 下拉至 GND4 |

所以 CAN 版本仍需要处理 `MFC_EN2`，但它不再作为逐帧的 RS485 收发方向信号使用，而是服务于 CAN 收发器的工作模式控制。

**待实板核对：这不是发射极直接接地的普通反相光耦电路。** 输入低、光耦截止时，两个输出分别由上拉和下拉电阻决定；输入高、光耦导通时，集电极与发射极间导通。如果 R100、R113 均按图中的 4.7 kΩ 装配，可能出现分压，不能直接假定 MFC/EN 能可靠拉低或 CAN/STB 能可靠拉高。需核对实际装配并测量两个输出电压，对照 U17/U22 的输入门限，之后才能确定驱动层的有效电平。

`MFC_RES2` 经 U7、U24 控制 R138（120 Ω），电阻跨接公共总线，RS485 和 CAN 模式均适用。它是终端电阻控制信号，不是 MCU、流量计或 MCP2515 的复位信号。终端配置按总线位置确定，不因板上有三个插座就启用三份终端。

## 4. MCU 通信引脚记录

以下引脚号以原理图 U9 的 64 引脚符号为依据，FSP 外设复用和 PCB 实物仍需在建工程时复核。

| 用途 | 网络 | MCU 端口 | U9 引脚 |
| --- | --- | --- | --- |
| 上位机 CAN 发送 | CAN_TXD0 | P103 | 45 |
| 上位机 CAN 接收 | CAN_RXD0 | P102 | 46 |
| 上位机收发器模式控制 | RS485_EN0 | P500 | 49 |
| 上位机终端电阻控制 | RS485_RES0 | P501 | 50 |
| 上位机备用 UART 发送，当前不用 | RS485_TXD0 | P101 | 47 |
| 上位机备用 UART 接收，当前不用 | RS485_RXD0 | P100 | 48 |
| 流量计 RS485 发送 | MFC_TXD2 | P302 | 30 |
| 流量计 RS485 接收 | MFC_RXD2 | P301 | 31 |
| 流量计收发器控制 | MFC_EN2 | P304 | 28 |
| 流量计终端电阻控制 | MFC_RES2 | P303 | 29 |
| MCP2515 SPI 主出从入 | SPI MOSI | P109 | 34 |
| MCP2515 SPI 主入从出 | SPI MISO | P110 | 35 |
| MCP2515 SPI 时钟 | SPI SCK | P111 | 36 |
| MCP2515 SPI 片选 | SPI NSS | P112 | 37 |

这里的 SPI 是电路网络功能名，尚未指定使用哪个 FSP SPI 驱动实例。应核对上述管脚的实际复用能力，确定外设 SPI 模式及片选 GPIO 配置，不能凭网络名直接写成 SPI0。

## 5. 编译配置建议

当前先规划两种固件组合：

| 固件组合 | 上位机侧 | 流量计侧 | 状态 |
| --- | --- | --- | --- |
| 方案 A | MCU 内置 CAN0 | SCI2 + RS485 + Modbus RTU 主站 | 用户已确认协议；设备寄存器表、串口参数及硬件待确认项尚需补齐 |
| 方案 B | MCU 内置 CAN0 | SPI + MCP2515 + CAN | 硬件路径已确认；实际流量计 CAN 型号及协议尚缺 |

建议配置语义如下，尚未创建实际固件配置文件：

```c
/* 设计示意，实际名称可在建工程时统一。 */
#define HOST_LINK_CAN       1
#define MFC_LINK_RS485      1
#define MFC_LINK_CAN        2
#define MFC_PROTOCOL_MODBUS_RTU  1

#define HOST_LINK          HOST_LINK_CAN
#define MFC_LINK           MFC_LINK_RS485
#define MFC_PROTOCOL       MFC_PROTOCOL_MODBUS_RTU
```

编译时只包含选定的流量计传输后端及对应设备协议。上例为 RS485 + Modbus RTU 配置；切换为 CAN 时必须选择其实际设备协议，不能沿用 RTU 配置。协议组合不匹配、设备映射未配置或 CAN 协议未实现时应在构建/配置校验阶段报错，避免生成表面可用的空实现。不要在运行时创建两个流量计通信任务再选择其中一个。J9/J15/J10 的线束选择不改变任务结构。

将业务层与设备协议、传输驱动分开：业务层使用统一的设定流量、读状态、关断等操作；协议层编码设备命令；后端实现 RS485 事务或 MCP2515 CAN 报文收发。启动状态和上位机设备信息中应能查到固件版本及编译选中的流量计模式。

**当前 RS485 协议以用户确认的 Modbus RTU 为准。** 已有专用 ASCII 手册与当前协议不一致，后续需补充实际设备的 Modbus 寄存器资料。板卡具有 CAN 选项，不等于设备已支持 CAN；方案 B 仍需取得对应型号的 CAN 波特率、节点地址、报文定义、参数缩放和错误处理规则，不能自行把 RTU 帧装入 CAN 就视为设备可用协议。

## 6. FreeRTOS 初步任务方案

对应流程图见 [主板软件运行流程-FreeRTOS.md](<doc/Design Document/主板软件运行流程-FreeRTOS.md>)，包括编译与启动、三个任务协作、独立的 RS485 Modbus RTU 通信流程与状态图、整机控制状态和阀门互斥切换。

建议使用三个业务任务；六台流量计使用六个通道对象，由一个通信任务管理。FreeRTOS 的 Idle 等系统任务不计入这三个业务任务。

| 任务 | 建议相对优先级 | 触发方式 | 主要职责 |
| --- | --- | --- | --- |
| ControlTask | 高 | 控制命令、故障通知、状态机期限 | 唯一的九阀输出写入者；处理启停、联锁和过程状态；向流量计任务发请求 |
| MfcTask | 中高 | 请求队列、轮询期限、UART 通知或 SPI 状态轮询 | RS485 版本运行 Modbus RTU 主站状态机；独占选定后端，管理六通道、超时重试、采集及离线检测 |
| HostCanTask | 中 | MCU CAN 接收通知、回复与上报请求 | CAN 报文解析、命令合法性检查、提交控制请求、返回设备快照及执行结果 |

此优先级关系是初步建议。MfcTask 必须限制单次处理量并适时阻塞，使上位机命令不会被持续采集饿死；流量计 CAN 版本还应验证最坏调度延迟不会造成接收缓冲溢出。

运行流程：

1. 复位后尽早设置阀门及阀组电源为经硬件确认的安全输出状态；初始化时钟、GPIO 和基础驱动。
2. 按编译配置初始化上位机 CAN、选定的流量计后端、收发器控制和终端策略。初始化不应长时间等待流量计在线。
3. 创建静态队列、同步对象、三个任务，启动调度器。
4. MfcTask 在调度器启动后逐台识别和配置流量计，建立在线状态、量程、单位和有效数据时间戳；ControlTask 管理初始化及预热状态；HostCanTask 始终提供状态查询和停机入口。
5. 就绪后接受启动或设定请求。HostCanTask 先确认请求收到，ControlTask 执行联锁和过程逻辑；需要设备交互时交给 MfcTask，完成后再返回执行结果。
6. MfcTask 周期采集，发布结果或故障事件；ControlTask 根据状态决定后续动作；HostCanTask 响应查询并按已定义协议上报变化。
7. 停机或故障时优先处理外部阀门和待执行命令，再异步请求流量计内部关闭及零设定；不得等串口或 CAN 事务超时后才处理外部阀门。

任务间通过有界队列传递请求和结果，状态快照使用短临界区或适当同步。ControlTask 不阻塞等待设备通信。中断只做必要的数据搬运、状态记录和允许的 FreeRTOS 通知，不执行复杂协议解析或阻塞调用。

RS485 由 UART 中断通知数据与发送完成，MfcTask 运行“空闲、组帧、等待帧间隔、发送、等待应答、校验、完成/异常/重试”的 Modbus RTU 状态机。精确的字符/帧间隔由适当精度的硬件计时或接收时间戳实现，不能直接用粗粒度任务延时代替。上位机使用 MCU 内置 CAN 中断；MCP2515 在本版接线下使用 SPI 状态轮询。

关键停机/故障请求应有不会被普通采集请求挤掉的锁存标志或独立通知机制；普通队列满时返回忙或失败。看门狗依据关键任务心跳和期限健康状态统一管理，不能仅因 Idle 仍运行就一直喂狗。具体监控放置及超时值在调度预算确认后确定。

## 7. 六路流量计管理

每个通道对象至少包含：通道号、Modbus 从站地址、启用状态、设备寄存器映射、气体配置、量程、单位、数据格式与缩放、目标流量、实际流量、内部阀门状态、报警、在线状态、最后有效应答时间、最后有效采样时间和故障计数。

### 7.1 Modbus RTU 主从关系与帧处理

- MCU 为总线唯一主站，六台流量计为从站。建议从站地址为数值 `1` 至 `6`，实际配置待确认；RTU 地址为单字节数值，不再发送三位 ASCII 地址字符串。第六路未启用时跳过，不作为必要在线条件。
- 默认全部使用单播请求，以便逐台确认；暂不启用广播控制。每条总线同时仅保留一个在途事务。
- RTU 帧为“从站地址 + 功能码 + 数据 + CRC16”，CRC 低字节先发。按帧长度、CRC、从站地址、功能码和当前事务的预期响应结构检查报文。
- 接收使用 RTU 帧间隔和字符间隔规则；具体时序见流程文档第 3 节。串口速率、校验位、停止位必须匹配实际设备，不再沿用旧 ASCII 手册的 9600/8N1 假设。
- 正常回应才更新对应参数的有效值与时间戳；合法异常回应单独记录异常码，不等同于设备离线。不相关或校验失败的帧不能延长当前事务的绝对超时期限。
- 停机所需内部阀关断和零流量设定改为对应设备的 Modbus 写操作。寄存器未确认前，不填入猜测地址，也不发送原 ASCII 命令；通信失败时仍由外部阀门优先关断。

### 7.2 设备寄存器映射待填

| 业务参数 | 所需操作 | 待确认内容 |
| --- | --- | --- |
| 流量设定 | 写设定值，必要时读回确认 | 功能码、地址、数据类型、寄存器数量、缩放、范围 |
| 实际流量 | 周期读取 | 功能码、地址、单位、符号、浮点/定点格式、多寄存器字序 |
| 内部阀门控制 | 设置正常控制或关闭 | 线圈/寄存器类型、地址、控制值及重复执行语义 |
| 内部阀门状态 | 读取确认状态 | 地址、位定义；不等同于外部 V1–V9 状态 |
| 报警与运行状态 | 周期读取 | 地址、报警位定义、故障清除方式 |
| 量程、单位、数字控制模式 | 初始化读取/设置，若设备支持 | 地址、编码、写入条件及是否保存到非易失存储 |

常见候选功能码包括读保持寄存器 0x03、读输入寄存器 0x04、写单寄存器 0x06、写多寄存器 0x10。它们仅用于协议设计参考，实际支持范围由设备手册决定；寄存器显示编号与报文中的零基地址偏移也需确认。规则依据 [Modbus 应用协议](https://www.modbus.org/file/secure/modbusprotocolspecification.pdf)。

### 7.3 轮询、失败与恢复

- MfcTask 按“关断请求优先、控制请求其次、周期读取随后”选择事务，并在正常情况下公平轮询启用通道。每个等待状态均阻塞等待通知或期限，不持续空转。
- 超时或传输错误采用有界重试。仅对明确允许重复执行的操作自动重试；写请求超时只能说明结果未知，不能断言设备没有执行。
- 达到通信失败阈值后标记该通道离线、使相关数据过期，继续服务其他通道；以较低频率探测恢复。合法异常响应中的非法地址/功能等配置错误直接报告，不通过重复重试伪装成链路问题。
- RTU 没有事务编号。超时后应清理接收状态并进入与设备最大响应时间匹配的恢复窗口；结合地址、功能码和预期长度处理迟到帧。不能仅靠软件请求编号就宣称完全消除了迟到帧歧义。
- 停止后撤销旧开阀和非零设定事务，旧结果不能重新推进启动。内部写操作应在完整帧边界和有限事务期限内调度，外部关断不等待总线腾空。
- 采集周期由实际帧长、字符位数、波特率、响应时间、帧间隔和重试预算共同确定，不预先承诺六路毫秒级刷新。
- 气路图中的 H2、CH4、N2、O2、CO2 及流量标注仍需逐台核对铭牌和工艺；量程、缩放和设定上限不从图中直接推定。既有操作手册记载至少 15 分钟预热，适用性随实际 Modbus 型号复核；预热状态不阻塞通信和停机。

## 8. 九阀控制与过程状态

V1 至 V6 为支路外部阀；V7、V8、V9 为混气管后的通路阀。MCU 软件统一控制外部阀，流量计内部阀状态作为另一类参数记录。

按用户最新说明，将 V7、V9 作为同一个逻辑阀组 `ValveGroup79`，明确联锁规则如下（1 为开，0 为关）：

```text
V7 = V9
V7/V9 阀组打开 ⇒ V8 必须关闭
V8 打开 ⇒ V7、V9 必须同时关闭

合法条件：(V7 == V9) && !(V8 && (V7 || V9))
```

按 `(V7,V8,V9)` 排列，只允许以下三个目标组合；其余组合均不允许：

| V7 | V8 | V9 | 含义 |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 三阀全关，也作为互斥切换的中间目标 |
| 1 | 0 | 1 | V7/V9 阀组打开，V8 关闭 |
| 0 | 1 | 0 | V8 打开，V7/V9 阀组关闭 |

ControlTask 统一执行组操作：

- 打开 V7 或 V9 的请求都解释为“打开 V7/V9 阀组”：先关闭 V8，满足关闭条件并再次通过联锁后，再同步下发 V7、V9 开命令。
- 关闭 V7 或 V9 的请求都解释为“关闭 V7/V9 阀组”，同时下发两阀关命令；不会因此自动打开 V8。
- 打开 V8 时，先同步关闭 V7、V9，等待两阀均满足关闭条件后，再打开 V8。关闭 V8 不会自动打开 V7/V9。
- 批量设置的完整目标必须通过上述合法条件，显式指定 V7/V9 不一致或两组同时打开的请求应拒绝。单阀指令先转换成组操作，再生成合法目标；不能分别放行两个互相冲突的开阀请求。
- 初始化、自动过程、手动控制及故障处理都使用同一组目标和联锁入口；关闭 V7/V9 时撤销该组未完成的打开请求。

切换互斥通路采用“先关后开”：`101 → 000 → 010` 或 `010 → 000 → 101`。等待由非阻塞状态机管理，期间仍响应停止与故障；真正打开前再次检查请求有效性和联锁。

V7/V9 的目标值在同一次组操作中更新，输出层同步提交两阀命令，不将它们作为两个独立业务动作。这里的同步是软件命令要求；机械动作时间、输出更新偏差和反馈能力仍需实板确认。不能把“GPIO 已改变”直接报告为“机械阀已到位”；当前资料尚未确认九阀具有位置反馈。

建议状态包括 INIT、WARMUP、READY、STARTING、RUNNING、STOPPING、FAULT。所有开阀路径都应经过 ControlTask 联锁；上位机的手动指令也不能绕过。异常后撤销未完成的开阀及非零流量请求，避免恢复通信时执行旧命令；复位或故障解除后不自动恢复送气。

安全停机暂按“撤销开阀请求、关闭供气相关外部阀、异步请求流量计关断”设计。是否需要主动排空、V7/V8/V9 的停机位置及阀组电源处理顺序，需由气路工艺明确，不能默认所有场景下开排空阀或全部断电就是最终安全状态。

## 9. 上位机协议与资源约束

上位机通信固定为 CAN。需要定义设备标识、协议版本、启停、六路设定、阀门操作、状态查询和报警等报文；CAN 波特率、标准/扩展 ID、分包、心跳周期和超时策略尚待确定。涉及多帧命令时，应完整重组和校验后再执行，不能边收边开阀。

状态上报至少区分：请求已接收、操作已执行、设备已确认。状态快照应包含过程状态、九阀命令状态、六路在线状态及参数、数据有效标志、数据时间戳和故障码；没有物理反馈时不伪造阀门实际位置。

MCU 项目资源基线为 48 MHz Cortex-M4F、256 KB Flash、32 KB SRAM、8 KB Data Flash。FreeRTOS 先采用静态任务栈和有界缓冲区，按链接映射及实测栈水位核算内存；避免六路各建一套任务与大缓冲。配置变更才持久化，不周期写入运行采样值。

用户提出整机待机功耗小于 100 W、环境温度 −10 ℃ 至 +40 ℃。这些是整机验证目标，不能仅凭使用 RTOS 或 MCU 的规格判定满足；需结合六台流量计、阀门、供电和散热验证。流量计自身允许的使用温度及准确度条件也应单独核对。

## 10. 待确认项与联调顺序

| 项目 | 当前状态及下一步 |
| --- | --- |
| 上位机装配 | U5 已焊、U2 未焊来自用户说明；核对 U4 两侧信号及 R8/R9/R11/R13 装配 |
| 流量计 RS485 极性 | 核对 R14/R17/R148/R149 的实际装配，逐端口做通断检查并形成线束表 |
| MFC_EN2 电路 | 测量 U7 的集电极/发射极、U17 DE、U22 STB 在两种输入下的电压，核对是否满足门限，必要时由硬件修正 |
| 收发器实际型号与供电 | U17 标注 SP3485EN、供电网络标为 5V4；核对实物型号与额定供电兼容性，上位机预留 U2 同类项也需记录 |
| 接地与终端 | 核对 GND3/GND4/GND5/FG、J12 MFC-ISO 的实际连接；确定两条总线各自的终端位置与控制极性 |
| MCP2515 接线 | 确认实板是否与图一致，INT 是否未接、Y1 是否 8 MHz，以及 SPI 实际外设复用 |
| 流量计 CAN 协议 | 当前未提供。取得实际 CAN 型号及协议前，仅保留方案，不宣称 EX201 CAN 通信已具备 |
| 流量计 Modbus RTU 参数 | RTU 已由用户确认；补齐波特率、校验/停止位、六台从站地址、功能码、寄存器表、数据缩放与设备最大响应时间；旧 ASCII 手册不能替代 |
| 设备参数 | 核对六台地址、型号、气体、量程、单位、接口选件及模拟强制控制端的接法 |
| 阀门与工艺 | 核对 V1–V9 对应线束、常开/常闭属性、有效电平、阀组电源控制、动作时间、反馈及各模式动作顺序 |
| 上位机 CAN 协议 | 确定波特率、ID、请求/响应、状态上报、心跳与失联后的停机规则 |
| 实时性与资源 | 确定允许的控制延迟、采集刷新周期、离线判定时限，再确定任务周期、优先级、队列和栈大小 |

联调建议依次为：断电核对跳线、线束和装配 → 测量使能与终端控制 → 上位机 CAN 单链路验证 → 单台流量计通信与关断验证 → 六台总线负载和掉线验证 → 九阀联锁及中途停机验证 → CAN 版本在协议齐备后单独验证 → 长时间运行、复位、通信异常与环境测试。

当前验证范围为原理图阅读、引脚和网络交叉核对，以及设备资料核对；尚未进行实板测量、固件编译或运行测试。

## 11. 记录更新

| 日期 | 更新 |
| --- | --- |
| 2026-09-10 | 建立 READ.md；将上位机改为固定 CAN；明确三个流量计端口共用跳线选择的 RS485/CAN 总线；撤销以太网假设；修正 J10 引脚；记录 MCP2515 轮询方案、EN 电路待核对项及三个 FreeRTOS 业务任务方案 |
| 2026-09-10 | 增加 FreeRTOS 流程图文档，区分编译选择与运行流程，细化三任务协作、故障关断和阀门互斥切换 |
| 2026-09-11 | 按用户确认改用 RS485 + Modbus RTU 主站；撤销当前方案中的专用 ASCII 帧及指令；增加寄存器映射待填表、RS485 专用通信流程和状态图 |
| 2026-09-11 | 更新 V7/V8/V9 联锁：V7 与 V9 同步开关，其阀组与 V8 互斥；合法目标改为 000、101、010，同步修订通路切换流程 |
| 2026-09-11 | 软件设计草稿更新至 V0.3，同步实板接口、Modbus RTU、FreeRTOS 三任务及 V7/V9 阀组规则；更新五幅配图和目录，完成全部 22 页排版检查 |
