# 六路质量流量计控制系统工程记录

更新时间：2026-09-20。当前阶段：已接入上位机CAN_USER、六路EX201自动采集、流量写入/读回确认及九路电磁阀执行，尚未完成实板验证。

**当前工程版本：V1.7.1.260920。** 自 V1.1.1.260911 起，工程统一使用下述版本命名规则，并在本文件维护每个版本的变更记录。

当前代码运行索引：[主板运行流程与收发函数说明](<doc/Design Document/Design Statement/主板运行流程与收发函数说明.md>)。该文件集中记录启动、三个任务、六路轮询、CAN/RS485收发、流量写入、结果回复、异常恢复及实际使用的函数，作为后续阅读和维护流程的统一入口。

**当前任务通信规则：任务之间的命令、执行结果、六路测量状态、九阀状态及请求有效状态统一使用FreeRTOS队列按值传输。每个任务只读写自己的业务上下文；不得通过结构体指针、共享命令槽或调用另一任务的更新接口来跨任务交换业务数据。** 启动时共同创建、之后保持不变的队列句柄和静态存储属于基础配置。CAN/UART中断与所属任务的驱动缓冲保留现有实现。
**代码可读性要求：遵循[AGENTS.md](AGENTS.md)。** 使用可搜索、可单步调试的直接C代码；自定义结构体按值嵌套最多两层，业务成员访问最多两次跳转。各模块状态独立静态存放，初始化明确绑定同任务依赖，跨任务仍只传队列消息。

## 版本命名与变更记录

版本格式为 `V主版本.次版本.修订版本.YYMMDD`，例如 `V1.1.1.260911`。前三段为正常递增的版本号，最后六位为该版本形成日期，`260911` 表示 2026 年 9 月 11 日。

- 主版本：整体架构或不兼容接口发生重大调整时递增。
- 次版本：新增功能或较大的方案扩展时递增。
- 修订版本：问题修复、细节调整或文档补充时递增。
- 默认采用以上递增规则；主版本或次版本递增时，其后的数字归零；用户指定的版本号优先。本次起始版本固定为 `V1.1.1.260911`。
- 每次形成新版本，必须同步更新本文件的当前版本号，并在下表新增日期、变更内容、关联文件及验证状态；保留历史条目，不覆盖旧版本记录。
- 同一天可以形成多个版本，通过前三段区分；日期使用各版本的实际形成日期，不因打开文件或跨日查看而改变。后续正式交付文件的版本标识采用同一格式。
- 每次版本更新完成并通过相应检查后，由 Codex 提醒提交推送并提供建议提交名称；用户自行执行提交和推送。除非用户再次明确要求代为操作，否则不执行 Git 提交或推送，也不重复弹框询问。

| 版本 | 日期 | 变更内容 | 关联文件及验证状态 |
| --- | --- | --- | --- |
| V1.7.1.260920 | 2026-09-20 | 按功能整理源码目录：src仅保留四个入口文件，A_System/A_Control移至App，A/F/H_Valve移至Valve；同步Debug/Release编译目录、头文件路径、VS Code、测试脚本和文档索引。 | 源码内容保持不变；构建及回归验证结果见本版末节。 |
| V1.7.0.260919 | 2026-09-19 | 新增A/F/H_Valve九阀执行：启动关闭、三组共享12V吸合200ms后约5V保持、V7/V9同步及V8互斥；Control与CAN新增阀状态覆盖队列；V7统一联动入口，V9只读，参数表升至2，新增0x13阀GPIO错误；修正P401/P402为GPIO。 | 阀门GPIO/计时、CAN执行及原六路轮询/写入回归通过；ARM构建及资源占用见本版末节。实装MOS、线圈和机械时序未实板验证。 |
| V1.6.1.260915 | 2026-09-15 | 按AGENTS.md降低嵌套：独立存放任务/六路通道/EX201事务和CAN驱动状态；删除CAN空包装结构及队列创建转发函数；展开压缩分支，新增CAN请求状态枚举和步骤注释，更新阅读与调试索引。 | 24个自定义结构体按值嵌套不超过两层，生产成员访问不超过两次；原轮询、写入、队列、六种任务顺序及CAN协议测试通过，增加缺少依赖存储绑定时的拒绝测试；ARM构建通过，text=30108、data=2468、bss=8324。无实板验证。 |
| V1.6.0.260915 | 2026-09-15 | 所有任务间业务交接统一为七个静态FreeRTOS队列：CAN→Control→MFC命令及反向结果、六路完整遥测覆盖队列、两个独立CAN请求状态队列；任务独占各自上下文，保持CAN_USER线上格式和参数表版本1。 | 六路轮询、写链路、CAN协议及新增队列所有权/覆盖/拥塞/状态过期测试通过；RA4M1 Debug构建通过，text=30564、data=320、bss=10432字节。CAN故障通过队列传播，状态有效期20ms，实板调度与栈水位待验证。 |
| V1.5.0.260915 | 2026-09-15 | 接通CAN_USER流量写请求→ControlTask→静态命令队列→MfcTask→RFSM确认/单次WSFD写入/RSFD读回→静态结果队列→CAN回复；新增A_Control及写执行状态机，保留六路轮询；执行期限改为3000ms，新增0x10～0x12业务错误码。 | 六路写入、分辨率和量程、错误应答、写入已生效但ACK丢失、读回不一致、取消旧请求、队列复制/拥塞、慢响应与时间回绕测试通过；原轮询及CAN测试通过；RA4M1 Debug无新增警告，text=29860、data=320、bss=8904字节。实板、九阀执行、下行CAN及双协议探测未验证或未实现。 |
| V1.4.0.260915 | 2026-09-15 | 新增A_MFC六路EX201初始化及周期采集，001～006全部轮询；共享唯一事务、每路独立元数据/流量/故障缓存、公平调度、离线重试和硬件恢复；A_System组织任务上下文并向CAN发布真实参数。 | 六台模拟仪器集成测试及原CAN_USER测试通过，覆盖串路、掉线恢复、全部缺席、协议异常、硬件失败和节拍回绕；RA4M1 Debug构建无新增警告，text=27180、data=320、bss=8632字节。无电路，实机未验证；写执行、九阀输出、下行CAN及双后端探测仍待实现。 |
| V1.3.0.260914 | 2026-09-14 | 基于用户CAN_USER代码接入CAN0：保留扩展ID、8字节布局、自定义校验及读写功能码；新增A_HostCan、F_CanUser、F_HostCan，扩展H_HostCan；建立六路参数、九阀目标与业务结果接口；同步FSP扩展帧和邮箱配置。 | 本机协议、ISR队列、异步回复、联锁目标、拥塞和异常恢复测试通过；RA4M1 Debug构建无新增警告，text=23288、data=320、bss=8112字节；CAN分析仪及实机未验证。当前执行入口默认未就绪，不自动操作MFC或阀门，六通道采集与ControlTask执行仍待接入。 |
| V1.2.2.260914 | 2026-09-14 | 增加 `RMFS`、`RDPP`、`RFRU`、`RFSM`、`RVSS`、`RCVS`、`RALM` 七个只读业务接口；新增 `A_EX201_DeviceInfo` 和逐字段有效标志；功能层增加一至四位无符号十进制数据解析。 | 七条请求指令、设备参数解析、有效标志累积和越界响应测试通过；RA4M1 Debug 构建通过，`text=18548`、`data=16`、`bss=6968` 字节；尚未完成自动初始化序列和实机验证。 |
| V1.2.1.260914 | 2026-09-14 | 建立 EX-201S 的 `A_`、`F_`、`H_` 分层代码及上下文传参方式；增加 `WSFD` 设置流量、`RSFD` 读取设定流量、`RCFR` 读取瞬时流量、`WVSS=2` 关闭流量接口；增加四位流量尾数和带符号瞬时流量转换；补充自编代码命名与逐层调用规则；清理并忽略已废弃的历史设计文件。 | EX-201S 协议与事务主机测试通过；RA4M1 Debug 构建通过，`text=18548`、`data=16`、`bss=6968` 字节；尚未完成 EX-201S 实机通信和量程、小数位、单位初始化读取。 |
| V1.2.0.260911 | 2026-09-11 | MFC 下行取消预编译二选一，改为同一固件同时包含 SCI2/EX-201S 与 SPI1/MCP2515/CAN_USER，由 MfcTask 运行时探测、连续确认并锁定有效链路；链路失效时先安全停机再重新探测。同步 README、Word、流程 Markdown、两个 XMind 和两个 Visio。 | [V1.2.0 软件设计](<doc/Design Document/流量计控制系统软件设计-V1.2.0.260911.docx>)及本节列出的图文资料；已完成文档结构和版式检查。自动识别业务逻辑及实板双后端探测尚未实现和验证。 |
| V1.1.3.260911 | 2026-09-11 | 建立 FreeRTOS 三任务工程骨架；在 MfcTask 下新增 SCI2、SPI1 和 SPI DTC Stack；SCI2 配置为 EX-201S 使用的 9600/8N1，SPI1 配置为 MCP2515 使用的主机、Mode 0、1 MHz、SSL0 低有效；补充中断向量、引脚和最小回调；更新主板运行流程图。 | FSP 源配置与生成文件已核对；完成 Debug 命令行构建验证。当前仅为任务和通信外设骨架，EX-201S、CAN_USER、MCP2515 与业务任务逻辑尚未实现，尚未完成实板验证。 |
| V1.1.2.260911 | 2026-09-11 | 更正 MFC 通信：RS485 使用 EX-201S 自带 ASCII 协议，CAN 使用 CAN_USER；撤销原 Modbus 方案；同步 Word、流程 Markdown、两个 XMind 和两个 Visio，保持三任务及九阀联锁。 | 对应旧文档已从当前工作区清理，可通过 Git 历史提交 `cacbbf7` 追溯；该版本尚未完成固件与实板验证。 |
| V1.1.1.260911 | 2026-09-11 | 建立统一版本命名及记录制度，将当前六路流量计软件方案作为首个记录版本：上位机固定 CAN；MFC 下行 RS485 / CAN 编译选择并匹配跳线；RS485 方案采用 Modbus RTU；FreeRTOS 三个业务任务；V7/V9 同步且与 V8 互斥；补充 CAN 硬件通信和 EX201 专用 ASCII 手册说明。 | 对应 V0.4 补充稿和早期设计资料已从当前工作区清理，继续由 Git 历史保存；该版本尚未完成固件实现和实板联调。 |

此前的 V0.3、V0.4 为 Word 草稿历史修订号，不追溯改写为正式工程版本；当前工程版本以上述记录为准。

## 自编代码分层命名规则

### 源码目录分类

文件夹按功能模块分类，文件名的`A_`、`F_`、`H_`前缀区分应用、功能和硬件层级。每个模块的`.c`与配套`.h`放在同一目录。

| 目录 | 文件与职责 |
| --- | --- |
| `GS_flowmeter/src` | 仅保留`hal_entry.c`、`HostCanStack_entry.c`、`MfcTask_entry.c`、`ControlTask_entry.c`，负责启动和任务入口；`R_BSP_WarmStart`继续放在`hal_entry.c`。 |
| `GS_flowmeter/App` | `A_System.c/.h`、`A_Control.c/.h`：系统初始化、任务间队列交接与业务协调。 |
| `GS_flowmeter/Valve` | `A_Valve.c/.h`、`F_Valve.c/.h`、`H_Valve.c/.h`：阀门联锁、吸合保持时序与GPIO驱动。 |
| `GS_flowmeter/Can` | 上位机CAN通信与CAN_USER协议。 |
| `GS_flowmeter/Mfc` | 六路流量计管理与EX201通信。 |
| `GS_flowmeter/Bsp` | 当前为空，暂不新增内容。 |
| `GS_flowmeter/ra`、`ra_cfg`、`ra_gen`、`script` | 分别为厂商库、库配置、自动生成代码和链接脚本，保留现有组织。 |

### 文件与函数命名

自 2026-09-14 起，项目自行编写的 C 模块按职责使用固定前缀，源文件、头文件及其函数必须保持同一层级前缀：

| 层级 | 文件前缀 | 函数前缀 | 职责 |
| --- | --- | --- | --- |
| 用户/业务逻辑层 | `A_` | `A_` | 业务请求、事务状态机、通道管理和对任务提供的业务接口 |
| 功能逻辑层 | `F_` | `F_` | 协议编解码、数据转换、校验和不直接操作硬件的通用功能 |
| 硬件逻辑层 | `H_` | `H_` | MCU外设、GPIO、UART、SPI、CAN及具体硬件驱动 |

每个自行编写的 `.c` 文件必须配套同名 `.h` 文件，包括内部 `static` 函数在内的函数均使用所属层级前缀。例如，EX-201S 模块分为 `A_EX201.c/.h`、`F_EX201.c/.h`、`H_EX201.c/.h`。FSP 自动生成的 `ra_gen` 文件和任务入口文件保留配置器生成的名称，避免重新生成时被破坏；FSP 中允许配置的用户回调函数仍采用相应的 `H_` 前缀。

结构体与变量统一遵守以下规则：

- 结构体类型名应简明表达模块职责，各单词首字母大写并用下划线分隔，例如 `A_EX201_Context`、`F_EX201_Response`。结构体实例以小写 `g_` 开头，例如 `g_ex201_context`。
- 结构体成员使用能够表达用途的小写蛇形命名。用于保存到存储器的 `Save` 结构体，其单个成员不得超过 32 位，并且至少包含一个明确的 32 位成员，以保证按 32 位边界组织数据；数组元素的基本类型也不得超过 32 位。
- 仅在当前 `.c` 文件使用的普通文件级变量必须声明为 `static`，使用全小写 `s_` 前缀。模块运行状态不得拆成多个文件级变量；确有长生命周期需求时，将状态集中到一个 `g_` 结构体上下文，并由任务持有、通过指针传入各层接口。
- 尽量使用局部变量和函数参数传递数据。接口不得依赖隐藏的可写全局状态；上下文结构体必须由调用者创建，函数通过结构体指针读写状态，以便同一模块创建多个实例并便于移植。
- 头文件仅放置接口需要的宏、条件编译、类型定义和函数声明，不放置变量定义、函数实现、内联业务处理或其他执行逻辑。

函数依赖必须逐层调用：

```text
任务入口 → A_应用层 → F_功能层 → H_硬件层 → FSP/HAL
```

- `A_` 函数可以调用本模块的 `A_` 私有函数、`F_` 接口以及其他模块公开的 `A_` 接口，不得直接调用 `H_` 接口。
- `F_` 函数可以调用本模块的 `F_` 私有函数和 `H_` 接口，不得调用 `A_` 接口，也不得绕过 `H_` 直接操作 FSP 外设。
- `H_` 函数负责调用本模块的 `H_` 私有函数及 FSP/HAL 接口，不得反向调用 `F_` 或 `A_` 接口。
- FreeRTOS 任务入口负责持有业务上下文指针、提供系统节拍及调度 `A_` 接口；板级共享上下文统一由A_System静态持有并向任务提供指针，模块不自行查找全局状态。协议状态机不直接取得系统节拍，避免把操作系统依赖扩散到功能层和硬件层。

本项目使用 R7FA4M1AB3CFM 管理六台 EX-201S 系列质量流量控制器和九个外部电磁阀。流量闭环由流量控制器内部完成，MCU 负责流量设定、状态采集、气路过程协调及阀门联锁，上位机负责操作和监测。

本文将信息分为“原理图确认”“用户装配说明”“软件建议”和“待确认”。原理图不能替代实板装配和电平验证。当前软件代码版本为 V1.7.1.260920，Word、流程与架构图的已验证设计基线为 V1.2.0.260911；当前实现以本README及最新Markdown说明为准，历史文档用于追溯。

**2026-09-11 最新通信方案：MFC RS485 使用 EX-201S 仪器自带专用 ASCII 协议，MFC CAN 使用用户自定义 CAN_USER；同一固件同时保留两个后端，由 MfcTask 运行时探测、确认并锁定有效链路。此前 Modbus RTU 和预编译二选一方案均已被取代。** 两个 RJ45 和一个 DB9 仍共用一条下行总线，外接三选一；J17/J18 继续由人工统一选择物理总线。

**2026-09-11 阀门联锁更新：V7 与 V9 同步开关，组成一个逻辑阀组，与 V8 互斥。** 当前联锁以这条用户最新说明为准，具体规则见第 8 节。

**通信资料适用范围：** EX201 说明书485通讯.pdf 为当前 RS485 协议依据；用户提供的 E:/c/can_module 为CAN_USER通用报文基准，上位机侧已按其实现。当前参数表见 [CAN_USER通讯与参数表](<doc/Design Document/Design Statement/CAN_USER通讯与参数表.md>)；下行CAN兼容对端及设备参数映射仍需落实。RS485 标准为 9600 bit/s、8N1，OPTION 标签优先；命令、流量换算和手册歧义见第 7 节。

## 1. 依据与当前结论

| 依据 | 用途 |
| --- | --- |
| [CVD01(Q2)-GS260806CN-V2.0B2.0.pdf](<doc/CVD01(Q2)-GS260806CN-V2.0B2.0.pdf>) | 第 1 页：上位机接口；第 2 页：MCU、SPI 转 CAN；第 3 页：流量计接口、跳线及阀门驱动 |
| [EX201 操作说明书.pdf](<doc/科赋乐质量流量计说明书/EX201 操作说明书.pdf>) | 流量计接口、使用条件及预热要求 |
| [EX201 说明书485通讯.pdf](<doc/科赋乐质量流量计说明书/EX201 说明书485通讯.pdf>) | 当前 RS485 协议依据：ASCII 电文、累加校验和、指令与流量换算；按 OPTION 和实机核对参数及手册歧义 |
| [气路图](<doc/无标题.png>)及用户说明 | 六条气体支路、九阀用途与互斥要求 |
| 用户于 2026-09-10 提供的装配说明 | 上位机收发器位置只焊 U5；流量计外接端口按线束三选一使用 |
| 用户于 2026-09-11 最新更正的协议 | MFC RS485 使用 EX-201S 自带协议；MFC CAN 使用 CAN_USER |
| 用户于 2026-09-11 最新更正的链路策略 | 不再预编译选择 MFC 后端；根据有效 EX-201S 或 CAN_USER 通信在运行时识别并使用对应链路 |
| 用户于 2026-09-11 更新的阀门逻辑 | V7/V9 同开同关，二者组成的阀组与 V8 互斥 |

当前通信架构为：

| 通信方向 | 物理接口 | 总线与控制器 | 软件结论 |
| --- | --- | --- | --- |
| 上位机 ↔ MCU | J1、J2，两个 DB25 | 两口共用一路 CAN，MCU 内置 CAN0 + U5 | 当前装配固定使用 CAN，仅设一个上位机通信任务 |
| MCU ↔ 六台流量计 | J9、J15，两个 RJ45；J10，一个 DB9 | 三口共用一组 H/B、L/A；J17、J18 共同选择 RS485 或 CAN | 固件同时包含 EX-201S 与 CAN_USER 后端；MfcTask 运行时探测并锁定由跳线实际接通的有效协议 |

**本版原理图中的 RJ45 是串行总线连接器，没有显示以太网 MAC/PHY 或以太网接口电路，不按以太网、TCP/IP 或网口通信设计。**

两个上位机 DB25 属于同一条上位机总线；三个流量计端口属于同一条流量计总线。上下两条总线相互独立，即使两侧均使用 CAN，也不是直接连在一起的同一条 CAN 总线。

## 2. 上位机接口核对

### 2.1 连接关系

原理图第 1、2 页显示：

```text
上位机
  ↕
J1 / J2 DB25，共用 OUT/H/485B、OUT/L/485A
  ↕ 保护与滤波电路
U5：TCAN1042VDRQ1 CAN 收发器
  ↕ U4：ADUM1201 数字隔离
MCU：CAN_TXD0 / CAN_RXD0，内置 CAN0
```

原理图还画有 U2（SP3485EN）作为 RS485 路径。根据用户提供的实际装配情况，U2 未焊、U5 已焊，因此当前上位机接口按 CAN 使用。J1/J2 共用 U5，不能视为两个独立 CAN 通道；两个接口还带有电源等引脚，并非全部引脚都用于通信。

### 2.2 J1/J2 通信相关引脚

以下为原理图引脚编号；制作线束时须区分连接器插接面与焊接面的观察方向。

| 两个 DB25 的引脚 | 原理图网络 | 当前 CAN 用途 |
| --- | --- | --- |
| 2、15 | OUT/H/485B | CANH |
| 3、16 | OUT/L/485A | CANL |
| 1、14 | GND3 | 通信侧参考地 |
| 5、6、7、18、19、20 | 24VP | 电源网络，不能当通信脚使用 |

### 2.3 使能、终端电阻与装配注意点

- `RS485_EN0` 经 U3 控制 U5 的 STB。名称虽带 RS485，当前上位机 CAN 装配仍需初始化此信号。
- `RS485_RES0` 经 U3、U6 控制 R16（120 Ω）终端电阻，根据本板在总线中的位置配置。
- U4 的 MCU 侧存在 CAN 引脚与备用 UART 引脚的电阻连接路径。须核对 R8、R9、R11、R13 的实际装配；CAN 使用期间，不能同时将备用 `RS485_TXD0` 配置为活动 UART 发送输出。
- U5 的 STB 低电平为正常通信，高电平为待机。MCU 到隔离侧的实际电平和上电默认状态需实测确认。芯片模式依据 [TI TCAN1042-Q1 数据手册，第 9.4 节](https://www.ti.com/lit/gpn/tcan1042-q1)。

## 3. 流量计接口核对

### 3.1 三个端口共用同一条总线

J9、J15、J10 的通信线均接到同一组 `H/B`、`L/A` 网络。三口之间没有接口选择开关，也没有各自独立的收发器。

用户要求的“RJ45 或 DB9，三选一”属于产品接线约定：使用其中一个端口接入六台流量计所在的总线。软件无需按 J9/J15/J10 分成三个驱动。其余端口在电气上仍连接同一条总线，不能另接一种协议的设备网络。

| 板端接口 | H/B | L/A | 其他相关引脚 |
| --- | --- | --- | --- |
| J9 RJ45 | 4 脚 | 5 脚 | 1、2 脚为 GND4；3、6、7、8 脚未接通信信号 |
| J15 RJ45 | 4 脚 | 5 脚 | 1、2 脚为 GND4；3、6、7、8 脚未接通信信号 |
| J10 DB9 | **1 脚** | **2 脚** | 3 脚为经保护的 24V5；4 脚为 GND5；5 脚为 FG；6、7、8、9 脚未接 |

**修正：J10 的 L/A 是 2 脚，7 脚未接。** 不能沿用此前将 7 脚写为通信脚的记录。

流量计说明书中的设备 RJ45 为 1/2 脚 TR_COM、4 脚 TR−、5 脚 TR+。板端 H/B、L/A 的命名不能直接代替器件极性核对，须结合下面的 0 Ω 电阻装配确认线束。

板上 J10 的定义也不能套用流量计本体 DB9 的定义：设备 DB9 承担电源和模拟控制等信号，不能假设一根 DB9 直通线即可完成数字通信。选用板端 DB9 时，应单独出具至设备通信接口的线束表。GND4、GND5、FG 的连接按隔离和接地方案核对，不默认短接。

### 3.2 J17/J18 的选择关系

| 工作方式 | J17 短接 | J18 短接 | 连接的收发器 |
| --- | --- | --- | --- |
| CAN | 1–2 | 1–2 | U22 的 CANH / CANL |
| RS485 | 2–3 | 2–3 | U17 经电阻配置后的 H_485B / L_485A |

J17 的 2 脚、J18 的 2 脚分别进入公共滤波与保护电路，再连到三个端口。两只跳线需要人为设置一致，并非机械联动开关。跳线方向以 PCB 的 1 脚标识为准，不能仅凭“上面/下面”判断。

图中没有用于直接读取 J17/J18 状态的 MCU 检测信号，因此固件不能读取跳线位置，也不能通过软件改变跳线连接。固件可以通过完整且校验正确的 EX-201S 应答或 CAN_USER 报文间接识别有效链路，但识别结果不能代替生产和调试时对“跳线、器件装配、设备协议、线束”的人工核对。

U17 的 A/B 另有 0 Ω 电阻换向配置：R17 + R148 对应 B→H_485B、A→L_485A；R14 + R149 对应交叉连接。须核对实际焊接，四只不能全部导通，否则会将 A/B 相连。

### 3.3 RS485 路径

当前协议为 EX-201S 仪器自带的专用 ASCII 协议。MfcTask 作为主站事务调度者，经 SCI2 控制 RS485 收发；六台流量计作为从站响应请求。

```text
MCU SCI2：MFC_TXD2 / MFC_RXD2
  ↕ U21 ADUM1201
U17 SP3485EN
  ↕ A/B 电阻配置
J17、J18 均短接 2–3
  ↕ 公共滤波、保护电路
J9 / J15 / J10 → 六台流量计的共享总线
```

`MFC_EN2` 用于收发控制相关电路；`MFC_RES2` 控制共用终端电阻。半双工发送必须等待 UART 最后一个停止位真正发送结束，再按经实板验证的控制电平切换接收，不能仅以发送缓冲区为空作为切换条件。

### 3.4 CAN 路径

```text
MCU SPI：MOSI / MISO / SCK / NSS
  ↕
U11 MCP2515-I/ST：独立 CAN 控制器
  ↕ MFC_CTXD / MFC_CRXD，经 U23 ADUM1201
U22 TCAN1042VDRQ1：CAN 收发器
  ↕
J17、J18 均短接 1–2
  ↕ 公共滤波、保护电路
J9 / J15 / J10 → 实现 CAN_USER 的下行对端总线
```

- U11 完成 SPI 与 CAN 控制器之间的接口功能，U22 完成 CAN 物理层收发，两者功能不同。
- U11 的 Y1 标为 **8 MHz**。CAN 位时序计算应使用该振荡器频率，不能套用常见 16 MHz 模块的参数。
- 第 2 页中 U11 的 **INT（13 脚）未画出到 MCU 的连接**，RX0BF/RX1BF 同样未接。因此当前板型先采用 SPI 定时读取接收/错误状态的方案，不能假定已有 MCP2515 外部中断通知。
- U11 RESET（19 脚）由 R26/C44 构成上电复位相关电路，未显示 MCU 复位控制 GPIO。
- MCP2515 是经典 CAN 控制器。它只有两个接收缓冲区，SPI 轮询周期须结合 CAN 波特率、过滤规则、最大突发报文量和任务调度延迟验证。不能因为收发器支持更高能力就将本系统定义为 CAN FD。依据 [Microchip MCP2515 数据手册](https://ww1.microchip.com/downloads/en/DeviceDoc/MCP2515-Stand-Alone-CAN-Controller-with-SPI-20001801J.pdf)。

### 3.5 MFC_EN2 与 MFC_RES2：两个后端都需要保留

原理图第 3 页中，`MFC_EN2` 经 U7 同一只光耦晶体管影响两个**不同的电气节点**：

| U7 节点 | 网络与作用 |
| --- | --- |
| 12 脚，集电极 | MFC/EN，经 R98 到 U17 的 DE 和 /RE；R100 上拉至 5V4 |
| 11 脚，发射极 | CAN/STB，到 U22 的 STB；R113 下拉至 GND4 |

所以 CAN 链路仍需要处理 `MFC_EN2`，但它不作为逐帧的 RS485 收发方向信号使用，而是服务于 CAN 收发器的工作模式控制。运行时探测会在两种控制需求之间切换，必须先通过实板测量确认两个节点的有效电平和安全切换顺序。

**待实板核对：这不是发射极直接接地的普通反相光耦电路。** 输入低、光耦截止时，两个输出分别由上拉和下拉电阻决定；输入高、光耦导通时，集电极与发射极间导通。如果 R100、R113 均按图中的 4.7 kΩ 装配，可能出现分压，不能直接假定 MFC/EN 能可靠拉低或 CAN/STB 能可靠拉高。需核对实际装配并测量两个输出电压，对照 U17/U22 的输入门限，之后才能确定驱动层的有效电平。

`MFC_RES2` 经 U7、U24 控制 R138（120 Ω），电阻跨接公共总线，RS485 和 CAN 模式均适用。它是终端电阻控制信号，不是 MCU、流量计或 MCP2515 的复位信号。终端配置按总线位置确定，不因板上有三个插座就启用三份终端。

## 4. MCU 通信引脚记录

以下引脚号以原理图 U9 的 64 引脚符号为依据，FSP 外设复用和 PCB 实物仍需在建工程时复核。

| 用途 | 网络 | MCU 端口 | U9 引脚 |
| --- | --- | --- | --- |
| 上位机 CAN 发送 | CAN_TXD0 | P103 | 45 |
| 上位机 CAN 接收 | CAN_RXD0 | P102 | 46 |
| 上位机收发器模式控制 | RS485_EN0 | P500 | 49 |
| 上位机终端电阻控制 | RS485_RES0 | P501 | 50 |
| 上位机备用 UART 发送，当前不用 | RS485_TXD0 | P101 | 47 |
| 上位机备用 UART 接收，当前不用 | RS485_RXD0 | P100 | 48 |
| 流量计 RS485 发送 | MFC_TXD2 | P302 | 30 |
| 流量计 RS485 接收 | MFC_RXD2 | P301 | 31 |
| 流量计收发器控制 | MFC_EN2 | P304 | 28 |
| 流量计终端电阻控制 | MFC_RES2 | P303 | 29 |
| MCP2515 SPI 主出从入 | SPI MOSI | P109 | 34 |
| MCP2515 SPI 主入从出 | SPI MISO | P110 | 35 |
| MCP2515 SPI 时钟 | SPI SCK | P111 | 36 |
| MCP2515 SPI 片选 | SPI NSS | P112 | 37 |

这里的 SPI 是电路网络功能名，尚未指定使用哪个 FSP SPI 驱动实例。应核对上述管脚的实际复用能力，确定外设 SPI 模式及片选 GPIO 配置，不能凭网络名直接写成 SPI0。

## 5. 运行时链路识别方案

上位机固定为 MCU 内置 CAN0。MFC 固件同时包含两套总线后端及其匹配的应用协议，由 MfcTask 独占两个外设并管理运行时识别：

| 候选链路 | MFC 传输后端 | 应用协议 | 物理接通条件 |
| --- | --- | --- | --- |
| RS485 | SCI2 经 U21、U17 | EX-201S 自带 ASCII 协议 | J17/J18 均 2–3 |
| CAN | SPI1 经 MCP2515、U23、U22 | 用户自定义 CAN_USER | J17/J18 均 1–2 |

```c
typedef enum
{
    MFC_LINK_DETECTING = 0,
    MFC_LINK_RS485,
    MFC_LINK_CAN,
    MFC_LINK_FAULT
} mfc_link_type_t;
```

总线和协议仍是一一匹配关系，不允许 RS485 选择 CAN_USER 或 CAN 沿用 EX-201S 串口电文。探测阶段只能发送读取状态、读取设备信息等无副作用命令，并保持外部阀关闭和流量设定为零。RS485 不能仅靠被动监听，需发送只读 EX-201S 请求；CAN 通过轮询 MCP2515 接收缓冲并按 CAN_USER 识别规则确认，必要时发送协议规定的只读发现请求。

只有完整通过协议校验的报文才能计为一次链路确认，建议连续确认两次后锁定。锁定后不因单个异常报文切换；连续失败达到门限时，MfcTask 先通知 ControlTask 安全停机并撤销旧事务，安全状态确认及重探测延时到期后才解除锁定。若两种协议都被有效确认、J17/J18 不一致或无法建立确定链路，则进入配置故障，不采用简单的“先到先得”。

统一业务层使用设定流量、读取状态和关断接口；EX-201S 或 CAN_USER 层负责编解码；UART 或 MCP2515 层只负责传输。版本信息应同时报告工程版本、当前链路状态、锁定依据和最后确认时间。MFC CAN_USER 与上位机 CAN0 应用协议分别管理。

## 6. FreeRTOS 初步任务方案

对应流程图见 [主板软件运行流程-FreeRTOS.md](<doc/Design Document/Design Statement/主板软件运行流程-FreeRTOS.md>)，包括运行时链路探测与锁定、三个任务协作、RS485 EX-201S 通信流程与状态图，以及 CAN_USER 流程、整机控制状态和阀门互斥切换。

建议使用三个业务任务；六台流量计使用六个通道对象，由一个通信任务管理。FreeRTOS 的 Idle 等系统任务不计入这三个业务任务。

| 任务 | 建议相对优先级 | 触发方式 | 主要职责 |
| --- | --- | --- | --- |
| ControlTask | 高 | 控制命令、故障通知、状态机期限 | 唯一的九阀输出写入者；处理启停、联锁和过程状态；向流量计任务发请求 |
| MfcTask | 中高 | 请求队列、探测/轮询期限、UART 通知及 SPI 状态轮询 | 独占 SCI2 与 SPI1/MCP2515；探测并锁定 EX-201S 或 CAN_USER；管理六通道、超时重试、采集、离线检测及安全重探测 |
| HostCanTask | 中 | MCU CAN 接收通知、回复与上报请求 | CAN 报文解析、命令合法性检查、提交控制请求、返回设备快照及执行结果 |

此优先级关系是初步建议。MfcTask 必须限制单次处理量并适时阻塞，使上位机命令不会被持续采集饿死；流量计 CAN 链路还应验证最坏调度延迟不会造成接收缓冲溢出。

运行流程：

1. 复位后尽早设置阀门及阀组电源为经硬件确认的安全输出状态；初始化时钟、GPIO 和基础驱动。
2. 初始化上位机 CAN0、SCI2、SPI1/MCP2515、收发器控制和终端策略。初始化不应长时间等待流量计在线。
3. 创建静态队列、同步对象、三个任务，启动调度器。
4. MfcTask 在调度器启动后监听 CAN_USER，并按地址发送无副作用的 EX-201S 只读探测；连续确认后锁定有效链路，再逐台识别和配置流量计。ControlTask 在探测期间保持安全输出；HostCanTask 始终提供状态查询和停机入口。
5. 就绪后接受启动或设定请求。HostCanTask 先确认请求收到，ControlTask 执行联锁和过程逻辑；需要设备交互时交给 MfcTask，完成后再返回执行结果。
6. MfcTask 周期采集，发布结果或故障事件；ControlTask 根据状态决定后续动作；HostCanTask 响应查询并按已定义协议上报变化。
7. 停机或故障时优先处理外部阀门和待执行命令，再异步请求流量计内部关闭及零设定；不得等串口或 CAN 事务超时后才处理外部阀门。

任务间通过有界队列传递请求和结果，状态快照使用短临界区或适当同步。ControlTask 不阻塞等待设备通信。中断只做必要的数据搬运、状态记录和允许的 FreeRTOS 通知，不执行复杂协议解析或阻塞调用。

RS485 由 UART 中断通知数据与发送完成，MfcTask 推进 EX-201S 组帧、发送、接收至 CR、累加校验和检查、OK/NG 处理及有界恢复。应答超时与残帧恢复使用独立期限。上位机使用 MCU 内置 CAN 中断；MFC CAN_USER 使用 SPI 轮询 MCP2515，服务期限独立于六路采集周期。

关键停机/故障请求应有不会被普通采集请求挤掉的锁存标志或独立通知机制；普通队列满时返回忙或失败。看门狗依据关键任务心跳和期限健康状态统一管理，不能仅因 Idle 仍运行就一直喂狗。具体监控放置及超时值在调度预算确认后确定。

## 7. 六路流量计管理

每个通道保存通道号、协议类型、设备 ID、启用状态、气体、量程、单位、小数位、目标值、已确认值、实际流量、内部阀状态、报警及有效数据时间戳。外部阀的命令状态与仪器内部阀状态分别维护。两种通信方式均由一个 MfcTask 管理六个通道对象。

### 7.1 EX-201S 电文

标准串口 9600 bit/s、8N1，实机 OPTION 优先。MCU 是唯一请求发起方，拟用 ASCII ID 001–006，逐台确认。请求和响应如下：

```text
请求：@ + ID(3字符) + 命令(4字符) + 数据 + 校验和(2字符) + CR
应答：% + ID(3字符) + 命令(4字符) + OK/NG + 数据 + 校验和(2字符) + CR
```

校验和为起始字符至数据末尾全部字节之和的低 8 位，再转为两位十六进制 ASCII。CR 是 0x0D；例 @001WVSS155<CR> 的校验字段为 55，%001RVSSOK1CF<CR> 为 CF。同一时刻只有一个在途请求；有效 OK 更新对应参数，有效 NG 报告拒绝，无效帧不更新缓存、不延长绝对应答期限。

### 7.2 EX-201S 命令及实现边界

| 业务 | 命令 | 处理要求 |
| --- | --- | --- |
| 量程、小数位、单位 | RMFS / RDPP / RFRU | 初始化读取；流量等于尾数乘以 10 的负小数位次方 |
| 数字控制来源 | RFSM / WFSM | 0 数字、1 模拟；按允许条件设置并读回 |
| 实时流量 | RCFR | 按符号、小数位与单位解码，保存采样时间 |
| 数字设定值 | RSFD / WSFD | 四位十进制尾数，检查量程；写后确认，必要时读回 |
| 当前设定 | RCFS | 与数字 SP0 区分，结合控制来源核对 |
| 内部阀命令 | RVSS / WVSS | 0 全开、1 调节控制、2 全关；不可把全开当作正常调节 |
| 内部阀状态、开度 | RCVS / RCVO | 与外部 V1–V9 的命令状态分开记录 |
| 设备报警 | RALM | 0 正常；1 传感器，2 阀门过热，4 设定记忆回路，组合值相加 |

- 本项目不使用 Modbus。接收以起始字符和 CR 确定帧边界，不使用 CRC16 或 RTU 的 t1.5/t3.5 判帧。
- UART 发送缓冲空不等于最后停止位完成，必须等待真正发送结束后转接收。应答期限、最大帧长和残帧恢复均有界，超时值依据实机响应能力确定。
- 有效 NG 表示设备拒绝本次操作，不能直接判离线；NG 具体原因若报文未提供，不得编造错误码。只有匹配且校验通过、数据结构正确的 OK 才更新相应业务值。
- 写应答丢失时设备可能已经执行。按命令的幂等性决定先读回还是重试；禁止重复执行未确认可重复的配置操作。
- 协议没有线上事务序号；同地址同命令的迟到应答可能与新事务混淆。超时后按设备最大响应时间留恢复窗口，内部请求号仅用于阻止旧结果推进新的过程。
- 手册存在 ID 范围 01–99 与 001–009、RCFR 数据长度、写指令“响应：无”与通用 OK/NG 格式的歧义，联调时逐项向厂家或实机确认。
- 模拟强制阀输入可能优先于数字设定，必须核对接线和数字控制权。停机先由 ControlTask 关闭外部供气阀，再由 MfcTask 调度内部关闭和零设定。

当前代码已提供 `A_EX201_SetFlow`、`A_EX201_ReadSetFlow`、`A_EX201_ReadActualFlow`、`A_EX201_CloseFlow` 及对应的异步结果读取接口，并提供 `RMFS`、`RDPP`、`RFRU`、`RFSM`、`RVSS`、`RCVS`、`RALM` 七个设备信息读取接口。每次只读事务通过 `A_EX201_GetDeviceInfoResult` 更新 `A_EX201_DeviceInfo` 的对应字段和有效标志。流量值仍使用手册定义的“流量尾数”；六通道管理模块取得小数位和单位后再统一生成工程值。`A_EX201_SetFlow` 只发送 `WSFD`，不会隐式修改 `WFSM` 控制来源或 `WVSS` 阀门模式；`A_EX201_CloseFlow` 明确发送 `WVSS=2`。

### 7.3 CAN_USER 协议边界

MFC CAN 应用层固定采用用户自定义 CAN_USER，规划由MfcTask经SPI操作MCP2515完成收发。通用协议基准已由E:/c/can_module提供，当前纯编解码实现F_CanUser不依赖硬件、可供后续下行适配复用；下行节点与六通道映射、波特率、设备参数地址、单位缩放、业务确认及兼容对端仍需确定。上位机参数表不自动等同于下行设备参数表。

经典 CAN 单帧最多 8 字节数据，必要时按用户协议实现分段、序号及重组超时。CAN 硬件 ACK 或发送完成不等于业务执行完成。对端必须实现同一 CAN_USER 协议，不推定现有 EX-201S 原生支持 CAN_USER，也不默认把 ASCII 电文直接封装成 CAN。

### 7.4 六路调度和恢复

关断优先，控制其次，采集兼顾各启用通道；事务、重试及接收恢复均有界。离线设备降低探测频率，健康通道继续服务。停止撤销旧开阀与非零设定，旧结果不推进新启动。RS485 耗时按电文长度、字符位数、波特率和设备处理时间计算；CAN_USER 按帧负载、SPI 服务期限和应用确认时间单独预算。

图中气体及流量标注须核对铭牌与工艺，设备预热按实际 EX-201S 说明执行，预热期间仍保留通信和停机入口。

## 8. 九阀控制与过程状态

V1.7.0已实现九阀电气控制和200ms吸合/保持，详细流程及函数见[九路电磁阀执行说明](<doc/Design Document/Design Statement/九路电磁阀执行说明.md>)。完整工艺过程、机械到位反馈、独立急停和通信中断后的整机停机策略仍为后续范围。

V1 至 V6 为支路外部阀；V7、V8、V9 为混气管后的通路阀。MCU 软件统一控制外部阀，流量计内部阀状态作为另一类参数记录。

按用户最新说明，将 V7、V9 作为同一个逻辑阀组 `ValveGroup79`，明确联锁规则如下（1 为开，0 为关）：

```text
V7 = V9
V7/V9 阀组打开 ⇒ V8 必须关闭
V8 打开 ⇒ V7、V9 必须同时关闭

合法条件：(V7 == V9) && !(V8 && (V7 || V9))
```

按 `(V7,V8,V9)` 排列，只允许以下三个目标组合；其余组合均不允许：

| V7 | V8 | V9 | 含义 |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 三阀全关，也作为互斥切换的中间目标 |
| 1 | 0 | 1 | V7/V9 阀组打开，V8 关闭 |
| 0 | 1 | 0 | V8 打开，V7/V9 阀组关闭 |

ControlTask 统一执行组操作：

- CAN通过V7地址0x0306控制V7/V9阀组：打开时先撤销V8电气输出，再同步打开V7/V9；V9地址0x0308改为只读。
- 关闭V7的请求同时关闭V7/V9，不会因此自动打开V8。
- 打开V8时，先同步撤销V7/V9电气输出，再打开V8。机械释放时间未确认，本版没有据此设定机械等待死区。关闭V8不自动打开V7/V9。
- 批量设置的完整目标必须通过上述合法条件，显式指定 V7/V9 不一致或两组同时打开的请求应拒绝。单阀指令先转换成组操作，再生成合法目标；不能分别放行两个互相冲突的开阀请求。
- 初始化、自动过程、手动控制及故障处理都使用同一组目标和联锁入口；关闭 V7/V9 时撤销该组未完成的打开请求。

切换互斥通路采用电气“先关后开”：`101 → 000 → 010`或`010 → 000 → 101`。200ms吸合由非阻塞状态机推进，GPIO错误时尝试关断并锁存故障；尚未完成的写请求期间其他CAN写返回BUSY，当前不能作为独立急停接口。

V7/V9 的目标值在同一次组操作中更新，输出层同步提交两阀命令，不将它们作为两个独立业务动作。这里的同步是软件命令要求；机械动作时间、输出更新偏差和反馈能力仍需实板确认。不能把“GPIO 已改变”直接报告为“机械阀已到位”；当前资料尚未确认九阀具有位置反馈。

建议状态包括 INIT、WARMUP、READY、STARTING、RUNNING、STOPPING、FAULT。所有开阀路径都应经过 ControlTask 联锁；上位机的手动指令也不能绕过。异常后撤销未完成的开阀及非零流量请求，避免恢复通信时执行旧命令；复位或故障解除后不自动恢复送气。

安全停机暂按“撤销开阀请求、关闭供气相关外部阀、异步请求流量计关断”设计。是否需要主动排空、V7/V8/V9 的停机位置及阀组电源处理顺序，需由气路工艺明确，不能默认所有场景下开排空阀或全部断电就是最终安全状态。

## 9. 上位机协议与资源约束

上位机通信固定为CAN_USER。V1.3.0已接入CAN0，沿用现有500 kbit/s配置，改为29位扩展数据帧、固定8字节；上位机Type_Header=0，主板Type_FLOW=0x0F，主板默认地址1。四段地址按用户要求分别为只读float32、只读uint32、读写float32、读写uint32；具体分配、错误码、限制和交接接口见 [CAN_USER通讯与参数表](<doc/Design Document/Design Statement/CAN_USER通讯与参数表.md>)。当前只处理定向0x01写、0x02读；周期查询由上位机重复发送读请求，广播、主动上报及心跳暂不启用。

HostCanStack已运行非阻塞接收、参数查表及回复；每次最多读16个参数并逐参数用0x07返回。写操作保存来源与内部请求号，等待ControlTask/MfcTask提交实际结果后用0x06回复。原协议没有通用读错误帧，本版读失败不伪造数值，通过0x010C～0x010E保存最近错误供查询。V1.5.0已接通六路流量写入并在RSFD读回一致后回复成功；九阀执行尚未接入，阀门写请求仍返回0x08未就绪。

状态上报至少区分：请求已接收、操作已执行、设备已确认。状态快照应包含过程状态、九阀命令状态、六路在线状态及参数、数据有效标志、数据时间戳和故障码；没有物理反馈时不伪造阀门实际位置。

MCU 项目资源基线为 48 MHz Cortex-M4F、256 KB Flash、32 KB SRAM、8 KB Data Flash。FreeRTOS 先采用静态任务栈和有界缓冲区，按链接映射及实测栈水位核算内存；避免六路各建一套任务与大缓冲。配置变更才持久化，不周期写入运行采样值。

用户提出整机待机功耗小于 100 W、环境温度 −10 ℃ 至 +40 ℃。这些是整机验证目标，不能仅凭使用 RTOS 或 MCU 的规格判定满足；需结合六台流量计、阀门、供电和散热验证。流量计自身允许的使用温度及准确度条件也应单独核对。

## 10. 待确认项与联调顺序

| 项目 | 当前状态及下一步 |
| --- | --- |
| 上位机装配 | U5 已焊、U2 未焊来自用户说明；核对 U4 两侧信号及 R8/R9/R11/R13 装配 |
| 流量计 RS485 极性 | 核对 R14/R17/R148/R149 的实际装配，逐端口做通断检查并形成线束表 |
| MFC_EN2 电路 | 测量 U7 的集电极/发射极、U17 DE、U22 STB 在两种输入下的电压，核对是否满足门限，必要时由硬件修正 |
| 收发器实际型号与供电 | U17 标注 SP3485EN、供电网络标为 5V4；核对实物型号与额定供电兼容性，上位机预留 U2 同类项也需记录 |
| 接地与终端 | 核对 GND3/GND4/GND5/FG、J12 MFC-ISO 的实际连接；确定两条总线各自的终端位置与控制极性 |
| MCP2515 接线 | 确认实板是否与图一致，INT 是否未接、Y1 是否 8 MHz，以及 SPI 实际外设复用 |
| MFC CAN_USER | 通用报文编解码已具备；补齐下行节点、波特率、设备参数映射、确认/心跳及兼容对端，完成MCP2515驱动和独立联调 |
| EX-201S 协议参数 | 核对实际 OPTION、串口、ASCII ID、RCFR 长度、写回应、量程换算与最大响应时间；不再索取 Modbus 寄存器表 |
| 设备参数 | 核对六台地址、型号、气体、量程、单位、接口选件及模拟强制控制端的接法 |
| 阀门与工艺 | 核对 V1–V9 对应线束、常开/常闭属性、有效电平、阀组电源控制、动作时间、反馈及各模式动作顺序 |
| 上位机 CAN 协议 | CAN_USER代码和四段参数表已接入；使用CAN分析仪验证500 kbit/s扩展帧收发，接入业务执行及真实快照；心跳和失联停机规则待确定 |
| 实时性与资源 | 确定允许的控制延迟、采集刷新周期、离线判定时限，再确定任务周期、优先级、队列和栈大小 |

联调建议依次为：断电核对跳线、线束和装配 → 测量使能与终端控制 → 上位机 CAN 单链路验证 → 单台流量计通信与关断验证 → 六台总线负载和掉线验证 → 九阀联锁及中途停机验证 → CAN_USER 版本按用户协议与兼容对端单独验证 → 长时间运行、复位、通信异常与环境测试。

当前验证范围包括原理图与设备资料核对、主机协议测试和 RA4M1 Debug 构建；尚未进行实板测量、流量计通信或整机运行测试。

## 11. 记录更新

| 日期 | 更新 |
| --- | --- |
| 2026-09-10 | 建立 READ.md；将上位机改为固定 CAN；明确三个流量计端口共用跳线选择的 RS485/CAN 总线；撤销以太网假设；修正 J10 引脚；记录 MCP2515 轮询方案、EN 电路待核对项及三个 FreeRTOS 业务任务方案 |
| 2026-09-10 | 增加 FreeRTOS 流程图文档，区分编译选择与运行流程，细化三任务协作、故障关断和阀门互斥切换 |
| 2026-09-11 | 按用户确认改用 RS485 + Modbus RTU 主站；撤销当前方案中的专用 ASCII 帧及指令；增加寄存器映射待填表、RS485 专用通信流程和状态图 |
| 2026-09-11 | 更新 V7/V8/V9 联锁：V7 与 V9 同步开关，其阀组与 V8 互斥；合法目标改为 000、101、010，同步修订通路切换流程 |
| 2026-09-11 | 软件设计草稿更新至 V0.3，同步实板接口、Modbus RTU、FreeRTOS 三任务及 V7/V9 阀组规则；更新五幅配图和目录，完成全部 22 页排版检查 |
| 2026-09-11 | V1.1.2.260911：撤销 Modbus 方案，MFC RS485 改为 EX-201S 自带协议，CAN 改为 CAN_USER；同步当前设计文档及架构流程图。以上旧记录仅供追溯，不代表当前方案。 |
| 2026-09-11 | V1.2.0.260911：取消 MFC 后端预编译选择；同一固件运行时探测 EX-201S 或 CAN_USER，连续确认后锁定；链路失效时先安全停机再重新探测；同步当前图文资料。 |
| 2026-09-14 | V1.2.1.260914：实现 EX-201S 分层通信基础模块和流量业务接口，增加上下文传参、流量尾数转换及主机测试；清理已废弃的历史设计文件。 |
| 2026-09-14 | V1.2.2.260914：增加七个 EX-201S 设备信息只读接口、统一设备信息结构体、逐字段有效标志和数据范围校验。 |

## 12. V1.2.0.260911 文件与验证记录

| 当前文件 | 本次修订 |
| --- | --- |
| [软件设计文档](<doc/Design Document/流量计控制系统软件设计-V1.2.0.260911.docx>) | V1.2.0 已验证的 Word 设计基线；补充运行时探测、确认、锁定、失效停机及安全重探测方案 |
| [FreeRTOS 运行流程](<doc/Design Document/Design Statement/主板软件运行流程-FreeRTOS.md>) | 将编译二选一改为双后端初始化及运行时链路状态机，补充探测安全约束 |
| [软件架构图](<doc/Design Document/软件架构图.xmind>)、[主板软件架构图](<doc/Design Document/主板软件架构图.xmind>) | 同步 MfcTask 双后端所有权、链路识别与锁定职责 |
| [硬件架构框图](<doc/Design Document/硬件架构框图.vsdx>)、[主板软件运行流程](<doc/Design Document/主板软件运行流程.vsdx>) | 同步连接器、链路探测流程及版本标识 |

V1.2.0.260911 之前的废弃设计文件已从工作区清理，仍可通过 Git 历史追溯。上表中的 V1.2.0.260911 文档继续作为当前已验证设计基线。

验证：Word 正文与表格已逐页检查，五张流程图已作为内嵌图形写入并逐图检查，目录设置为打开文档时自动更新；两份 XMind 与两份 Visio 已完成压缩包结构和关键文本检查。当前协议描述及状态图不再采用 Modbus 或预编译二选一，修订记录中的旧方案仅用于追溯。CAN_USER 具体报文字段仍待提供；V1.1.3 的 FSP 骨架已通过 Debug 构建，本次尚未实现自动识别业务逻辑，也未进行实板联调。

## 13. V1.2.1.260914 代码与验证记录

| 当前文件 | 本次修订 |
| --- | --- |
| [EX-201S 应用层](<GS_flowmeter/Mfc/A_EX201.c>)及[接口](<GS_flowmeter/Mfc/A_EX201.h>) | 通过调用者持有的上下文实现非阻塞事务；增加设置流量、读取设定流量、读取瞬时流量、关闭流量及异步结果读取接口 |
| [EX-201S 功能层](<GS_flowmeter/Mfc/F_EX201.c>)及[接口](<GS_flowmeter/Mfc/F_EX201.h>) | 增加四位流量尾数编码以及四位无符号、五位带符号流量尾数解码 |
| [EX-201S 硬件层](<GS_flowmeter/Mfc/H_EX201.c>)及[接口](<GS_flowmeter/Mfc/H_EX201.h>) | 保持 SCI2、RS485 方向控制和中断环形缓冲职责，不向应用层暴露 FSP 操作 |
| [.gitignore](<.gitignore>)及设计资料目录 | 忽略废弃历史资料目录和旧版本文件；已删除文件继续由 Git 历史保存 |

验证：EX-201S 协议测试覆盖请求编码、响应解析、校验和、流量尾数编码及正负瞬时流量解码；事务测试覆盖成功、NG、响应不匹配、发送与响应超时，以及 `WSFD`、`RSFD`、`RCFR`、`WVSS=2` 业务流程。RA4M1 Debug 构建通过，固件占用为 `text=18548`、`data=16`、`bss=6968` 字节。尚未完成实机串口时序、数字控制来源、满刻度约束及单位换算验证。

## 14. V1.2.2.260914 代码与验证记录

| 当前文件 | 本次修订 |
| --- | --- |
| [EX-201S 应用层](<GS_flowmeter/Mfc/A_EX201.c>)及[接口](<GS_flowmeter/Mfc/A_EX201.h>) | 增加七个只读命令、设备信息枚举、`A_EX201_DeviceInfo`、逐字段有效标志和统一异步结果解析接口 |
| [EX-201S 功能层](<GS_flowmeter/Mfc/F_EX201.c>)及[接口](<GS_flowmeter/Mfc/F_EX201.h>) | 增加一至四位无符号十进制 ASCII 解析，并复用于流量尾数解析 |

验证：主机测试覆盖 `RMFS=1234`、`RDPP=2`、`RFRU=1`、`RFSM=0`、`RVSS=1`、`RCVS=3`、`RALM=5`，确认七个有效标志累计为 `0x7F`；同时确认 `RDPP=4` 等越界响应返回协议错误且不污染设备信息。RA4M1 Debug 构建通过。下一步由六通道管理模块按顺序调度这些接口，并记录每台设备的初始化进度、失败次数和最后有效时间。


## V1.3.0 CAN_USER实现与后续接入

本版依据用户提供的E:/c/can_module进行移植，保留线上协议格式，去除旧项目无关业务并按A/F/H分层。

| 模块 | 职责 |
| --- | --- |
| [A_HostCan.c](GS_flowmeter/Can/A_HostCan.c)及配套头文件 | 参数表、六路与九阀快照、写请求交接、延迟回复及超时 |
| [F_CanUser.c](GS_flowmeter/Can/F_CanUser.c)及配套头文件 | 原CAN_USER扩展ID、8字节负载、小端数值与自定义校验编解码 |
| [F_HostCan.c](GS_flowmeter/Can/F_HostCan.c)及配套头文件 | 上位机CAN传输适配 |
| [H_HostCan.c](GS_flowmeter/Can/H_HostCan.c)及配套头文件 | FSP CAN0、ISR接收队列、非阻塞发送和恢复 |
| [CAN_USER通讯与参数表](<doc/Design Document/Design Statement/CAN_USER通讯与参数表.md>) | 参数地址、业务结果码、兼容范围及任务交接说明 |
| [本机测试](tests/can/run.ps1) | 使用FSP测试替身编译实际A/F/H源码，验证协议和异步边界 |

验证：原算法1024组校验对比、ID/字节序、完整地址端点、uint32高位、非法帧与接收溢出、六路批量读取、读失败诊断、延迟写回复及来源关联、自定义返回码、V7/V9目标联动和V8互斥、超时/迟到结果、时间回绕、执行入口撤销和发送拥塞/恢复通过。RA4M1 Debug构建通过，无新增警告；text=23288、data=320、bss=8112字节。未做实板验证。

V1.3.0时六通道采集与执行任务尚未接入；V1.4.0完成六路EX201采集和上下文共享，V1.5.0进一步接入流量写执行。V1.7.0已启用MFC和九阀执行能力，完整气路工艺过程仍待接入；不能将CAN接收或命令入队当成仪器执行成功。之前V1.2.0文档中“CAN_USER字段待提供”的描述属于历史基线，当前上位机协议以本节链接的参数表为准。

当时建议提交名称：`feat(can): V1.3.0.260914 接入CAN_USER通信与参数映射`。由用户自行提交推送。

## V1.4.0 六路EX201初始化及周期采集

本次按用户要求直接启用六路，不只查询一路，第六路也参加初始化、周期采集和离线检查。当前可运行的是SCI2/RS485上的EX-201S自带协议。MCP2515/CAN_USER下行后端和运行时双协议识别仍是后续工作，现有SPI回调不代表该后端已经实现。

| 模块 | 职责 |
| --- | --- |
| [A_MFC.c](GS_flowmeter/Mfc/A_MFC.c)及配套头文件 | 唯一EX201事务、六个独立通道、公平轮询、各路初始化和掉线恢复 |
| [A_System.c](GS_flowmeter/App/A_System.c)及配套头文件 | 静态持有任务上下文，按每路小数位转换工程值，向CAN发布变化快照 |
| [MfcTask_entry.c](GS_flowmeter/src/MfcTask_entry.c) | 配置六路地址，每1ms推进轮询及发布；硬件失败自动退避重试 |
| [六路轮询说明](<doc/Design Document/Design Statement/六路MFC轮询说明.md>) | 默认周期、指令、地址、状态和无硬件测试方法 |
| [六路集成测试](tests/mfc/run.ps1) | 用六台模拟设备执行实际A/F协议与轮询代码，再核验CAN_USER线上回复 |

默认地址为001、002、003、004、005、006，位于A_MFC_DefaultConfig。这是可修改的软件初值，不能代表六台实物已设置这些ID；也可以在任务入口调用A_MFC_Initialize前覆盖g_config.addresses。地址限定1～999且六路互不重复，不能用同一个默认仪器地址接满六台。

初始化逐通道交替读取RMFS、RDPP、RFRU、RCFR、RSFD、RFSM、RVSS、RCVS、RALM。就绪后每路RCFR目标查询间隔500ms，每路每1000ms轮换查询RSFD、RFSM、RVSS、RCVS、RALM中的一个，即每个慢速参数约5s刷新一次。以上为到期调度参数，不保证所有负载下的完成期限；共享9600波特率总线及超时会增加数据年龄，未实现并发六路发送。

失败立即撤销对应字段有效位；初始化失败，或就绪后连续三次事务失败，进入离线并撤销该路所有数据有效位。离线5s后重新读取元数据，健康路继续轮询。错误后静默50ms，再清理接收状态发送后续请求；迟到帧最长时间仍需实机核对。硬件恢复失败使六路离线，5s后重试硬件。

上位机周期发0x02读取0x0000～0x0005，MCU从六路缓存逐参数回复0x07；CAN读取不会临时占用RS485发起查询，也未新增主动上报功能。0x0102的bit0～bit5反映六路在线状态，0x0160～0x0165反映初始化状态。未采到的数据不标为有效，实际流量超过2000ms按原CAN规则拒绝读取。离线路导致整组查询失败时，可以先查询在线掩码，再分别读取健康路。

本机验证命令：

```powershell
./tests/mfc/run.ps1
./tests/can/run.ps1
```

已验证六路全部初始化、持续多周期查询、正负尾数与不同小数位换算、CAN连续六参数回复和0x3F在线掩码、单路掉线及重连重读元数据、坏校验/NG/非法参数/地址不匹配/发送超时、全部设备缺席、硬件初始化及恢复失败、CAN晚启动、32位节拍回绕和多路超时下的公平调度。模拟数据仅存在于tests，生产固件没有模拟应答或假流量回退。

新增业务单函数静态栈帧：A_System_ProcessMfc为96字节，A_MFC_Process为32字节；这些不是完整调用链或中断嵌套的栈峰值。任务栈仍为既有1024字节，六路上下文静态分配；栈水位、电气时序、真实响应延迟和六台设备长时间运行须待硬件具备后验证。

当时建议提交名称：`feat(mfc): V1.4.0.260915 实现六路EX201周期采集与CAN参数发布`。由用户自行提交推送。

## V1.5.0 CAN流量写入执行链路

上位机用原CAN_USER的0x01写0x0200～0x0205，目标仍为Type_FLOW、数值仍为该通道单位的float32。HostCanStack解析并保存请求，ControlTask领取后通过长度1的静态FreeRTOS队列按值传递A_MFC_Command；MfcTask通过另一条长度1的静态队列返回A_MFC_CommandResult。保持一笔未完成写请求，不引入原始CAN帧跨任务透传。

| 模块 | 本次职责 |
| --- | --- |
| [A_Control.c](GS_flowmeter/App/A_Control.c)及配套头文件 | 领取CAN写请求、命令入队、结果关联及CAN业务码映射；只启用MFC执行能力 |
| [A_MFC.c](GS_flowmeter/Mfc/A_MFC.c)及配套头文件 | 与轮询共用唯一事务；检查在线、初始化、量程及分辨率，执行RFSM→WSFD→RSFD |
| [A_System.c](GS_flowmeter/App/A_System.c)及配套头文件 | 持有静态队列内存，连接两个任务；检查原CAN请求有效性，发布确认目标后再提交结果 |
| [ControlTask_entry.c](GS_flowmeter/src/ControlTask_entry.c) | 每1ms推进控制交接，串口仍由MfcTask独占 |
| [流量写入执行说明](<doc/Design Document/Design Statement/流量写入执行说明.md>) | 队列、执行状态、目标有效性、错误和期限定义 |
| [A_WriteTest.c](tests/mfc/A_WriteTest.c)及配套头文件 | 六路CAN写入到EX201再到CAN回复的集成测试 |

写入前等待当前轮询事务结束，重新读取RFSM确认数字控制来源，然后单次发送WSFD，收到OK后读取RSFD。尾数一致才设置目标有效位并回复0x06/结果0x00；这不表示实际流量已经达到目标。输入超过分辨率时返回0x0D，不任意舍入目标；允许float32表示及乘法造成的小误差。示例：小数位1时12.3可写为0123，12.34拒绝。

执行期限从CAN接收时算起，由1500ms改为3000ms，覆盖排队、当前在途轮询、RFSM、WSFD及RSFD。新增业务码0x10下行协议错误、0x11驱动/恢复错误、0x12写后读回不一致；保留原线上帧格式、地址和参数表版本1。上位机回复等待时间建议至少3500ms。

已发出的写不能因CAN超时或bus-off而撤回，因此不自动重发。CAN请求撤销或过期后，不再启动后续写入；已经开始的事务中止后恢复轮询，迟到结果不能完成新请求。目标及读回有效位在尝试WSFD时撤销，只有实际读回相同尾数后才恢复目标有效位。普通轮询可以恢复仪器读回值，但不会伪造一次MCU写入成功。

本次不自动切换数字/模拟来源，不发送WVSS，不操作外部九阀，也未接入下行CAN/MCP2515。完整气路状态管理、九阀驱动和联锁后续另行接入；当前“写成功”严格指数字流量设定及读回确认。

验证：`./tests/mfc/run.ps1`同时执行原六路轮询和新增写链路测试；`./tests/can/run.ps1`保留原协议兼容测试。覆盖六路分别写入、轮询继续、零和满量程、NaN/Inf/负值/超量程/分辨率、执行前来源变化、NG、UART和协议错误、应答丢失、读回不一致、队列复制和拥塞、过期/旧结果/bus-off取消、静态队列创建失败、超过1500ms的慢应答及32位节拍回绕。测试模拟器仅在tests目录，不编入固件。

RA4M1 Debug构建通过；text=29860、data=320、bss=8904字节。静态单函数栈帧A_System_ProcessMfc为128字节，A_Control_Process为80字节，A_MFC_ProcessCommand为32字节；这些不是完整调用链峰值。三个任务保留1024字节栈，真实栈水位、临界区时长和设备通信仍待实板验证。

建议提交名称：`feat(control): V1.5.0.260915 接通CAN流量写入与EX201读回确认`。由用户自行提交推送，包含本版和上一版尚未跟踪的新增源码及测试文件。

2026-09-15 文档补充（R1，适用代码V1.5.0.260915）：新增[主板运行流程与收发函数说明](<doc/Design Document/Design Statement/主板运行流程与收发函数说明.md>)，集中整理完整运行调用链、收发阶段、队列与缓存、操作示例和调试观察点；按当前函数定义核对。本次为文档整理，固件版本和行为保持不变。建议提交名称：`docs: 汇总主板运行流程与收发函数调用链`。

## V1.6.0 任务业务统一通过队列交接

本次按用户要求替换V1.5.0的跨任务共享缓存、命令槽领取及直接结果提交。HostCanStack、ControlTask、MfcTask各自独占业务上下文；A_System_Initialize集中创建七个静态队列，启动成功后队列句柄不再改变。没有新增任务，也没有改变CAN_USER线上协议、参数地址或六路轮询周期。

| 队列 | 方向 / 内容 | 长度与满时处理 |
| --- | --- | --- |
| host_command_queue | CAN→Control，A_HostCan_Command | 1，满则返回BUSY |
| command_queue | Control→MFC，A_MFC_Command | 1，满则形成BUSY结果 |
| result_queue | MFC→Control，A_MFC_CommandResult | 1，MFC保留DONE等待 |
| host_result_queue | Control→CAN，A_Control_Host_Result | 1，Control保留pending_result等待 |
| telemetry_queue | MFC→CAN，A_System_Telemetry含六路、link、ready | 1，覆盖为最新完整快照 |
| control_state_queue | CAN→Control，A_Control_Host_State | 1，覆盖为最新请求状态 |
| mfc_state_queue | CAN→MFC，A_Control_Host_State | 1，覆盖为最新请求状态 |

遥测生产缓冲、队列存储和CAN接收缓冲分别持有独立副本；不能将六个单路消息轮流写入同一覆盖队列。CAN暂停期间六路采集继续，CAN缓存直到本任务出队才改变。CAN查询仍读取自己的最新缓存，上位机仍通过周期0x02查询取数，未增加主动上报。

命令路径：A_System_ProcessHostCan在CAN任务内提取请求并入host_command_queue，Control出队后检查状态、转换工程值并入command_queue，MFC出队执行RFSM→WSFD→RSFD。结果反向经过result_queue及host_result_queue，CAN任务自己调用A_HostCan_CompleteCommand后回复。确认数据先入遥测队列；CAN收到结果后再次检查遥测，防止抢占时序导致先回复成功、后更新数据。

CAN请求有效状态分别送给两个消费者，包含请求号、请求起点、更新时间和valid。A_Control_RequestValid同时校验消息匹配、20ms状态有效期和3000ms原请求期限。CAN故障/撤销需经CAN任务发布后才由其他任务获知，存在调度延迟；不再直接读取CAN硬件状态来保证“CAN任务尚未处理bus-off时立即禁止WSFD”。CAN不再更新状态时，MFC在状态超期后的下一次运行中退出。已经发出的WSFD不能撤回，也不自动重放。20ms配置及中断/任务调度时长需要实板核对。

相关源码：[A_System.c](GS_flowmeter/App/A_System.c)、[A_System.h](GS_flowmeter/App/A_System.h)、[A_Control.c](GS_flowmeter/App/A_Control.c)、[A_Control.h](GS_flowmeter/App/A_Control.h)、[CAN任务入口](GS_flowmeter/src/HostCanStack_entry.c)。A_HostCan的PublishChannel、PublishSystem、PublishMfcLink、SetExecutors、TakeCommand、CommandActive、CompleteCommand接口均限定CAN任务自身调用，其他任务必须先入队。外部九阀执行和下行CAN后端仍未接入。

运行文档R2：[主板运行流程与收发函数说明](<doc/Design Document/Design Statement/主板运行流程与收发函数说明.md>)；同时更新六路轮询说明、流量写入执行说明和CAN_USER参数表中的任务交接内容。V1.2.0流程图保留为历史方案并增加当前实现入口，不将其规划状态视为已实现。

验证：`./tests/mfc/run.ps1`和`./tests/can/run.ps1`通过。新增用例覆盖六路完整快照覆盖、CAN停止消费时缓存所有权、两个状态消费者独立出队、CAN命令队列满、Control到CAN结果队列满时保留结果、过期状态阻止WSFD及跨节拍回绕、三任务全部六种交替顺序；原六路轮询和写故障测试继续通过。bus-off取消用例按新语义验证CAN任务发布失效后禁止写入，另验证状态超期时不再使用旧授权。

RA4M1 Debug构建通过，无新增警告；text=30564、data=320、bss=10432字节，比V1.5.0增加1528字节bss。静态单函数栈帧：A_System_ProcessHostCan为64字节、A_System_ProcessMfc为80字节、A_System_ProcessControl为40字节、A_Control_Process为72字节；这些不是完整调用链或中断嵌套峰值。三任务栈仍各1024字节。硬件尚未具备，未做实板通信、队列调度延迟或栈水位验证。

建议提交名称：`refactor(rtos): V1.6.0.260915 统一任务间队列通信`。由用户自行提交推送。


## V1.6.1 按AGENTS.md整理代码可读性

本次以用户新增的AGENTS.md为准，优先让代码容易看懂、搜索和单步调试。保持现有七个任务队列、六路轮询周期、单笔写请求、RFSM→WSFD→RSFD执行顺序、超时及CAN_USER线上格式。

- A_System.c开头直接列出g_host_can、g_can_transport、g_mfc、g_mfc_channels、g_ex201_transaction、g_ex201_function等静态对象；g_system登记固定地址，不再把所有任务状态层层嵌入一个结构体。其他文件仍通过函数入口参数工作，跨任务消息仍按值入队。
- A_MFC_Context通过p_channels和p_transaction引用同任务的独立存储；A_EX201_Context通过p_function引用组帧状态；A_HostCan_Context通过p_transport引用驱动状态。初始化处的地址绑定长期有效，不能在运行中重绑或指向临时栈变量。
- F_HostCan_Context直接复用H_HostCan_Context类型，删除只有hardware成员的一层包装；A/F/H函数调用层级保留。
- A_System_Initialize内直接写出七处xQueueCreateStatic，每处标明发送任务、接收任务和消息含义；删除仅转发创建调用的A_System_CreateQueue。
- 展开单行if/else和switch中的多条操作；A_Control_RequestValid逐步检查消息存在、请求匹配、状态年龄和总期限。CAN请求状态改用IDLE、WAIT_QUEUE、WAIT_RESULT、COMPLETED枚举，数值仍为0～3。
- 在CAN、Control、MFC运行函数中添加按实际执行顺序的中文步骤注释，明确无消息、队列满、等待响应时为什么结束本轮。保留用户注释的意图，并将“Control返回查询结果”更正为“返回写入执行结果”；周期查询数据来自telemetry_queue。

建议从[运行流程文档第12节](<doc/Design Document/Design Statement/主板运行流程与收发函数说明.md>)开始，按任务入口→队列创建→CAN任务→Control任务→MFC轮询/写状态机→EX201收发阅读。该文档修订为R3，调试观察点改为可直接展开的独立变量。

验证：`./tests/mfc/run.ps1`、`./tests/can/run.ps1`通过。原六路轮询、异常恢复、写入读回、队列拥塞、状态超期及六种任务交替顺序测试继续运行；测试夹具使用独立静态存储，新增缺少p_channels/p_transaction/p_function/p_transport绑定时的拒绝用例。已检查24个自定义结构体，按值嵌套最大两层；生产C成员访问链最长两次。FSP/FreeRTOS自身结构不在自定义结构计数范围。

RA4M1 Debug构建通过，无新增警告；text=30108、data=2468、bss=8324字节，data+bss共10792字节，比V1.6.0增加40字节。显式静态地址初始化使部分对象从bss移入data，不能只看bss下降来判断RAM减少。单函数静态栈帧：A_System_ProcessHostCan为72字节、A_System_ProcessMfc为80字节、A_System_ProcessControl为40字节、A_Control_Process为72字节；并非完整调用链峰值。任务栈仍各1024字节，实板电气行为、栈水位和20ms状态时限仍未确认。

建议提交名称：`refactor: V1.6.1.260915 按AGENTS规范简化代码结构与注释`。由用户自行提交推送。


## V1.7.0 九路电磁阀执行

新增`Valve/A_Valve.c/h`、`Valve/F_Valve.c/h`、`Valve/H_Valve.c/h`，按应用联锁、功能计时、硬件GPIO三层组织。状态只由ControlTask拥有，各模块采用平铺结构和明确步骤注释。现有FSP启动引脚配置和阀初始化均输出关闭，P401/P402已修正为普通GPIO。

九阀分三组共用12V控制，每个新开阀吸合200ms后约5V保持；用户已允许同组保持阀短时重新承受12V。重复开已开阀不刷新计时。V7/V9同端口同步更新，与V8先关后开。机械释放死区未确认，当前只保证电气输出操作顺序。

CAN命令通过host_command_queue进入ControlTask，阀命令不进入MfcTask。新增第八个静态队列valve_state_queue，按值覆盖发布完整九阀状态；先发布快照再回执行结果。计时始终在结果队列重试之前推进，队列满不能延长12V。CAN侧保留原单笔写事务规则：吸合期间其他写返回BUSY，200ms结束后才回复开阀成功；当前不是独立急停通道。

CAN_USER帧格式保持兼容，参数表版本升至2：0x0306统一控制V7/V9；0x0308只读，写入返回0x05；0x0309仍为要求V7/V9一致的完整九阀掩码。新增0x13阀GPIO错误和0x0101 bit0故障位。GPIO错误后尽力关断并锁存，输出有效位撤销，0x0105仍可读失败状态2。

本地验证：tests/valve/run.ps1、tests/can/run.ps1、tests/mfc/run.ps1。覆盖九阀映射、199/200ms边界、同组错峰开阀、重复命令、V7/V9同步、互斥每一步、GPIO失败、CAN结果延迟、状态队列、背压、过期授权和六种任务顺序；原六路EX201轮询和写入回归通过。测试目录沿用.gitignore，不纳入版本控制。RA4M1 Debug构建通过，无新增警告：text=31784、data=2644、bss=8332字节，data+bss=10976字节；三任务栈各1024字节，实际调用峰值未测。命令行验证使用tmp/valve-tests/build_modules.mk补入新增源码；e2 studio中刷新工程、更新构建文件后即可识别src下的三个新C文件。

未做实板验证：Q7/Q14/Q21实装型号、12V与保持电压、线圈电流及允许重复升压时长、上电瞬态、常闭属性、机械释放时间、最坏调度延迟和任务栈峰值均待核对。软件计时不能在CPU/ControlTask停止运行时保证硬件自动撤销12V。已完成保持状态的通信中断停机策略、完整气路过程及下行CAN仍未实现。

运行详情：[九路电磁阀执行说明](<doc/Design Document/Design Statement/九路电磁阀执行说明.md>)、[主板运行流程与收发函数说明](<doc/Design Document/Design Statement/主板运行流程与收发函数说明.md>)、[CAN参数表](<doc/Design Document/Design Statement/CAN_USER通讯与参数表.md>)。

建议提交名称：`feat(valve): V1.7.0.260919 实现九阀吸合保持与CAN联动控制`。由用户自行提交推送。

## V1.7.1 源码目录整理

`src`仅保留四个入口文件；`A_System.c/.h`、`A_Control.c/.h`移至`App`；`A_Valve.c/.h`、`F_Valve.c/.h`、`H_Valve.c/.h`移至`Valve`。十个移动文件与整理前的Git内容逐一核对一致，业务逻辑、任务和硬件配置不变。Debug/Release均增加Valve源码目录以及App、Valve头文件搜索路径，VS Code配置、测试脚本和Markdown索引同步更新。V1.7.0记录中的src路径为当时布局，现以本节为准。

验证：`tests/valve/run.ps1`、`tests/mfc/run.ps1`（轮询和写入两组）、`tests/can/run.ps1`全部通过。e² studio命令行生成Debug/Release构建文件后，使用本机ARM GCC 10.3.1与GNU Make完成两种配置的编译和链接；临时工作区未注册工具链，验证时显式提供工具链PATH。Release临时输出目录补入同一RA4M1目标的Debug生成文件`memory_regions.ld`，未修改链接脚本或内存布局。两种配置均为text=31784、data=2644、bss=8332字节，与整理前Debug占用一致；各保留五条原有FreeRTOS警告，无新增警告。没有进行实板验证。

测试目录仍按现有`.gitignore`保留为本地验证文件。建议提交名称：`refactor: V1.7.1.260920 按功能整理App和Valve目录`。由用户自行提交推送。
